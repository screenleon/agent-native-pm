# Decisions

Active architectural and behavioral decisions for Agent Native PM.

When this file exceeds 50 entries or 30 KB, archive older entries to `DECISIONS_ARCHIVE.md`. The most recent archival pass was on 2026-04-27.

## 2026-04-27: Phase 3B PR-3 — candidate feedback fields + quality summary [agent:backend-architect]

- **Context**: Phase 3B PR-3 adds optional operator feedback on evaluated backlog candidates (`feedback_kind`, `feedback_note`) and a derived `QualitySummary` on planning runs so the UI can show acceptance-rate and pending-review counts.
- **Decision**: (1) Migration 034 adds `feedback_kind TEXT NOT NULL DEFAULT ''` and `feedback_note TEXT NOT NULL DEFAULT ''` to `backlog_candidates`. Existing rows get empty string — callers treat empty as "no feedback recorded". (2) Valid feedback kinds are an enumerated set in the store layer (`good_fit`, `poor_fit`, `needs_clarification`, `duplicate`); the store rejects unknown kinds with a typed error rather than silently storing garbage. (3) `QualitySummary` is computed on-the-fly in `PlanningRunStore.GetByID` via `computeQualitySummary`; it is NOT stored — re-computed on every fetch. Errors are non-fatal and logged. (4) `BacklogCandidateStore.Update` now accepts `FeedbackKind *string` and `FeedbackNote *string` nil-pointer semantics — nil means "leave unchanged", pointer to empty string means "clear". (5) Frontend `CandidateReviewPanel` adds an optional post-decision feedback row; acceptance-rate summary propagates to `ProjectOverviewTab`.
- **Constraints introduced**: (a) `feedback_kind` must be one of the enumerated values or empty string; the store validates before writing. (b) `QualitySummary` is not persisted — removing candidates or re-running planning will change counts. (c) `Update` on an already-applied candidate is still rejected (pre-existing constraint). (d) Migration 034 `.down.sql` drops the two columns via standard `ALTER TABLE DROP COLUMN` (SQLite pre-3.35 note: down migrations are hand-invoked only — not auto-applied by the runner). Tests: `TestCandidateFeedback_*` in `backlog_candidate_feedback_test.go` pass.

## 2026-04-27: Phase 3B PR-1 — Context Pack v2 wire contract + planning_context_snapshots + pack_id on planning_runs [agent:backend-architect]

- **Context**: Phase 3B PR-1 adds the v2 planning wire contract, a context snapshot store, and correlates every new planning run to a pack UUID.
- **Decision**: (1) `wire.PlanningContextV2` wraps `PlanningContextV1` with four new envelope fields: `pack_id` (UUID), `role`, `intent_mode` (analyze|implement|review|document), `task_scale` (small|medium|large), plus `source_of_truth []SourceRef`. `UpgradeV1ToV2` is the sole constructor — it preserves all V1 sub-fields verbatim and normalizes nil `source_of_truth` to an empty non-nil slice. Schema version is always `"context.v2"`. (2) New `scale` package provides `EstimateTaskScale(title, description)` with combined word-count thresholds (small < 100, medium < 300, large ≥ 300) and keyword overrides (`refactor`/`migrate`/`redesign`/`overhaul`/`architecture` → large regardless of count). The scale package imports `wire` and is not imported by `wire` — leaf constraint preserved. (3) Migration 032 adds `planning_context_snapshots` table with FK cascade on `planning_run_id`. Migration 033 adds `context_pack_id TEXT NOT NULL DEFAULT ''` to `planning_runs`. Both use `TEXT NOT NULL DEFAULT ''` for JSON columns (SQLite-compatible). (4) `CreateWithBinding` generates a second `uuid.New()` for `packID` and persists it as `context_pack_id` on INSERT. All SELECT column lists and `scanPlanningRun` are updated to include the new column. (5) `ContextSnapshotStore` (new file) provides `Save` + `GetByRunID`; not wired to any handler yet (PR-2 concern). (6) `main.go` instantiates `contextSnapshotStore` with a `_ =` blank-assignment so the compiler does not error until a handler consumes it in PR-2.
- **Constraints introduced**: (a) `wire` remains a leaf — no imports outside stdlib + `time`. (b) `scale.EstimateTaskScale` word count is computed on the combined lowercased `title + " " + description` string; callers must not pre-process the text. (c) Migration 032 `.down.sql` drops the table and index cleanly on both SQLite and Postgres; migration 033 `.down.sql` is a `SELECT 1` no-op (SQLite has no DROP COLUMN — consistent with the existing 033-family pattern). (d) `context_pack_id` on old rows is empty string (DEFAULT '') — callers must treat empty as "no snapshot yet". (e) The DECISIONS.md 2026-04-25 entry reserved migration 032 as a "PR-3 placeholder" (LLM router); that reservation is superseded by this PR which physically creates 032. The LLM router (Phase 6c PR-3) must use the next available number (034) if it needs a migration.
- **Source**: Phase 3B PR-1 task spec. Tests: `go build ./...` clean; `TEST_DATABASE_URL=sqlite go test ./internal/planning/... ./internal/store/...` all pass; `make lint` clean.

## 2026-04-27: Phase 6c PR-2 Copilot follow-up — error_kind split + connector parity [agent:backend-architect]

- **Context**: Copilot review on PR #27 raised six line-level findings post-merge-review-pipeline. Two of the six (a confidence-error sentinel split and a malformed-source error_kind split) and one critic-round-4 mandatory finding (connector-side parser drift) shipped together as a single follow-up commit.
- **Decision**: (1) `audit.ErrConfidenceNotAllowed` is a NEW sentinel for the non-router-with-confidence case; `audit.ErrInvalidConfidence` retains its router-specific text and is reserved for nil/out-of-range Confidence on `actor_kind=router` rows. Callers that need to distinguish between the two failure modes now have correct `errors.Is` semantics. (2) `models.ErrorKindRoleDispatchMalformed = "role_dispatch_malformed"` is a NEW dispatch error_kind for tasks whose source string is missing the `role_dispatch:<role>` suffix entirely (legacy tasks from before suffixes were required, or programming errors that emit just `role_dispatch`). It is structurally distinct from `role_not_found` (well-formed id, absent from catalog). The remediation hint points operators at the task source field rather than the role catalog. (3) `parseRoleIDFromSource` returns `(roleID, hasSuffix)` — empty suffix counts as malformed. (4) The connector-side parser in `connector/service.go RunOnceTask` was previously emitting `ErrorKind: "unknown"` for both branches; it now emits `role_dispatch_malformed` and `role_not_found` to mirror the server, so operators see consistent diagnostics regardless of which path drained the task. (5) Frontend `availableRolesError: string | null` is a sibling state to `availableRoles`; on `/api/roles` fetch failure the hook keeps `availableRoles=null` (preserving the loading sentinel) and populates the error string. PlanningTab plumbs both into `CandidateReviewPanel` and `CandidateRoleEditor`, which render an explicit "Failed to load roles: …" alert in the dropdown + chip area.
- **Constraints introduced**: (a) `parseRoleIDFromSource(source) → (string, bool)` is the new helper signature in `internal/store/task_store.go`; the previous single-return signature is retracted (only one caller existed). (b) Server and connector parsers MUST stay in lockstep; godoc on the server-side helper documents this and connector tests reference the same `models.ErrorKind*` constants. (c) `actor_audit.actor_kind` enum comment in migration 030 now lists all five values (user, api_key, router, system, connector); embedded `;` characters are forbidden in migration comments because the SQLite parser splits on them (re-tripped during this fix). (d) Phase 6c is greenfield — no live deployment carries `tasks` rows with `source = "role_dispatch"` (no colon), so the new `role_dispatch_malformed` error_kind has no historical backfill obligation; future operators encountering this kind in an observed task should investigate the candidate-applier code path that produced the malformed source.
- **Source**: PR #27 Copilot review (six line-level findings) + critic-round-4 (M1 connector parser drift, M2 backfill question, S1 missing malformed-branch test, S2 missing frontend-error-UI test, S4 server-push catalog assumption). All M+S findings addressed in commit subsequent to `72e066d`.

## 2026-04-26: Phase 6c PR-2 implementation refinements (post review-pipeline) [agent:backend-architect]

- **Context**: Phase 6c PR-2 implementation went through critic + risk-reviewer + /security-review pipelines. Risk-reviewer raised three HIGH findings (H1 audit table had no read endpoint; H2 the 2026-04-25 DECISIONS entry described an `UpdateExecutionRole(ctx, id, role, actor)` API name that diverged from the shipped `Update(id, req, actor)` shape; H3 stale-role tasks auto-fail silently with no operator-visible signal). Critic raised four MAJOR fixes that overlap (confidence rule asymmetric on non-router actors, api-key vs session-user actor confusion, frontend stale-warning races the catalog fetch, drain-loop unbounded under poisoned queues).
- **Decision**: PR-2 absorbs the H1/H2/M1/M3 + critic-mandatory fixes inline rather than deferring. Concretely: (1) `BacklogCandidate.execution_role_authoring` is a server-populated read projection of the latest `actor_audit` row, populated by `BacklogCandidateStore.EnrichWithAuthoring(ctx, candidates)` in the list / get-by-id / patch / apply handlers. Pre-Phase-6c rows have no audit history and surface as `null` — backfill is intentionally not done because the historical authoring is unrecoverable. (2) `audit.ActorInfo.Confidence` is rejected when `actor_kind != "router"` — non-router rows MUST have nil confidence so downstream "router decisions ≥ X" queries don't silently include user-set rows. (3) Two new `ActorKind` values: `api_key` (automation auth) joins the existing user/router/system/connector enum so the audit trail can distinguish session callers from API-key callers; the apply and PATCH endpoints route through `buildAuthoringActor(r, rationale)` which inspects `APIKeyFromContext` first then `UserFromContext`. (4) `ClaimNextDispatchTask`'s stale-role drain loop is bounded by `staleDrainCap = 16` per call — a poisoned queue (catalog rename hits N tasks) drains in batches of 16, yielding control between batches so a connector poll cannot wedge for O(N) writes. (5) Frontend `availableRoles` is now `RoleInfo[] | null`; the `null` sentinel suppresses the stale-role warning until `/api/roles` resolves, eliminating the false-positive flash on mount. The CandidateRoleEditor's stale-check additionally consults the static `KNOWN_ROLE_IDS` mirror so the chip's stale state is unambiguous even before the runtime catalog arrives.
- **Constraints introduced (additive to the 2026-04-25 entry)**: (a) `BacklogCandidateStore.Update(id, req, actor)` is the canonical mutator — the 2026-04-25 entry's `UpdateExecutionRole(...)` name was a pre-implementation sketch and is hereby retracted. (b) `ApplyToTaskWithMode(id, mode, executionRole, actor)` carries the role in its argument list (not implicitly in the candidate row); the candidate's `execution_role` column is updated and audited inside the apply transaction when (and only when) it changes. (c) Stale-role tasks are auto-failed by `claim-next-task` with `error_kind=role_not_found` and an `actor_kind=system` audit row; operators detect them by inspecting `tasks.dispatch_status='failed'` until Phase 6c PR-4 surfaces this in the activity stream. (d) `actor_audit` has no foreign-key cascade to subject rows: this is intentional (history-preserving append-only ledger), and supersedes the 2026-04-25 entry's "cascade-delete with subject row" line which was incorrect.
- **Source**: PR-2 branch `phase6c-pr2-authoring-audit` review pipeline — critic round 1 findings #1/#3/#5/#6, risk-reviewer findings H1/H2/H3/M1, /security-review (zero findings).

## 2026-04-25: Phase 6c v5.1 — L0 safety + authoring lifecycle + LLM router (suggest-only) + activity visibility [agent:feature-planner]

- **Context**: Phase 6b shipped role-dispatch end-to-end in code but the user-facing path is broken in three independent ways: (1) `execution_role` has no UI authoring surface, so the role_dispatch radio is permanently disabled (catch-22); (2) "auto-dispatch" is in name only — operators must still manually pick a role for every task; (3) connector activity during long task execution (backend-architect can run 90 min) is invisible to the frontend, making dogfood debugging impossible. Phase 5 §(g)/§(h) also flagged subprocess sandboxing as a Phase 6 blocker. The user explicitly rejected simple / phase-staged solutions: "一律不考慮簡單作法 我希望是完善的改動 而不是臨時性處理" (memory: `feedback_no_simple_approach`). Closing all four gaps in one phase is therefore in scope, even at ~15-day total cost.
- **Decision**: Phase 6c ships as **5 separate PRs**, each independently reviewable and rollback-able, but coherent as a single capability slice. **PR-1 (done)**: role catalog skeleton (hand-maintained `[]Role{...}` with per-role `DefaultTimeoutSec`) + L0 safety boundary in connector subprocess invocation (wall-clock timeout via `cmd.Cancel = SIGTERM` + `cmd.WaitDelay = 5s` escalation, output size cap via `boundedWriter`, JSON schema minimum validation, 4 new error kinds: `role_not_found`/`dispatch_timeout`/`output_too_large`/`invalid_result_schema`). **PR-2**: full authoring lifecycle — migration 030 adds generic `actor_audit` table (subject_kind/subject_id/field/old_value/new_value/actor_kind/actor_id/rationale/confidence) which is the **single source of truth** for execution_role authoring metadata (no denormalised columns on `backlog_candidates` per critic round 3 #1; frontend reads via helper `LatestAuthoring(subject_kind, subject_id, field)` joining against latest audit row); PATCH endpoint accepts `execution_role`; apply API extended with `execution_role` payload; catalog enforcement at four entry points (PATCH, apply, claim-next-task; suggest gates at validation in PR-3); CandidateReviewPanel rewritten so role_dispatch radio is always enabled with inline `<select>`; CandidateRoleEditor inline popover on candidate cards; stale-role warning when previously-suggested role no longer in catalog. **PR-3 (suggest-only, B2)**: LLM router as advisory meta-agent — new `backend/internal/prompts/meta/dispatcher.md` (category=meta, version 1, default timeout 60s; lives under `meta/` subtree per critic #10, drift test walks both `roles/` and `meta/`); `dispatcher.Service.Suggest` reuses PR-1's `invokeBuiltinCLI` for safety; `POST /api/backlog-candidates/:id/suggest-role` returns RouterResult without persisting; **Apply API NOT extended with `role_dispatch_auto` mode** — operator confirms suggested role manually then applies via `mode=role_dispatch` (audit row `actor_kind="operator"` even when suggestion came from router; this avoids premature auto-apply before router quality is dogfood-validated, per user pick B2); 1 new error kind (`router_no_match`); router output validated against catalog (role_id must be known or "no_match", confidence ∈ [0,1], reasoning ≤ 1024 chars with control-char sanitization); migration 032 reserved as PR-3 placeholder (per critic #9). **PR-4**: connector activity tracking — migration 031 adds `current_activity_json/at` snapshot column on `local_connectors`; ActivityReporter on connector emits phase transitions via **enqueue-not-overwrite** queue (per critic #5 — phase transitions in rapid succession all reach subscriber; same-phase step changes coalesce in 500ms window); phase enum is `idle/claiming_run/planning/claiming_task/dispatching/submitting` (no `routing` — that arrives in 6d with auto-apply); `POST /api/connector/activity` lightweight ingest endpoint; in-memory Hub broadcasts to SSE subscribers via unbuffered channels (slow clients auto-dropped); `GET /api/connectors/:id/activity-stream` SSE with 30s keepalive + `X-Accel-Buffering: no` header (C1: SSE retained vs polling-only); `GET /api/connectors/:id/activity` polling fallback; project-level aggregate `GET /api/projects/:id/active-connectors`; frontend `useConnectorActivity` hook auto-degrades SSE→polling→stale; `ConnectorActivityBadge` 3 density variants. **Activity does NOT write to actor_audit** (per critic #8 — 5+ phase transitions per task would drown the human-meaningful authoring trail; only latest snapshot persists). **PR-5**: dogfood (8 deliberate-trigger steps including activity SSE observation) + `docs/operating-rules.md` "Role-dispatch + visibility model" section + DECISIONS.md final + **archival pass** moving 2026-04-22-and-older entries to `DECISIONS_ARCHIVE.md` (per critic #11 — file already past 30KB threshold).
- **Alternatives considered**: (1) Defer authoring + router + activity to Phase 6d — rejected; the user explicitly stated dogfood today is broken without all three (see catch-22 analysis in plan v5 §1.1). (2) Single mega-PR — rejected; 15-day single PR is unreviewable; coherent slicing into 5 PRs preserves coherence while making each PR shippable in 1-5 days. (3) Apply-time-only authoring (no candidate edit + no audit) — rejected; user feedback explicitly requires comprehensive authoring (`feedback_no_simple_approach`); apply-time-only would force re-rework when Phase 6d's LLM planner pre-fills `execution_role` at candidate creation. (4) Polling-only activity (no SSE) — rejected at user pick C1; sub-second visibility is consistent with `feedback_no_simple_approach` even though 3s polling would technically suffice. (5) Sync `role_dispatch_auto` in PR-3 — **rejected at user pick B2 (critic round 3 #2)**; without dogfood data on router quality, auto-apply is premature optimization; PR-3 ships suggest-only and PR-6 (or 6d) lands auto-apply once PR-5 dogfood validates router accuracy. (6) Async `role_dispatch_auto` + webhook in 6c — deferred to 6d per user §5 Q3 answer (depends on auto-apply existing first). (7) Skip Role.Category field — rejected; without `category="meta"` filter the dispatcher prompt would surface in `/api/roles` and self-recommend, breaking the routing semantics. (8) `actor_audit` as candidate-specific table — rejected; designed generic from start (subject_kind discriminator) so PR-3 router-actor rows (when 6d auto-apply lands) and PR-4 system-actor rows reuse the same infrastructure. (9) Denormalised `execution_role_set_by/_at/_confidence` columns on `backlog_candidates` — **rejected at critic round 3 #1**; `actor_audit` is the single source of truth and frontend reads via JOIN helper to avoid drift between two writers. (10) Activity history written to `actor_audit` — **rejected at critic round 3 #8**; ~5 phase transitions per task dispatch would drown the human-meaningful authoring trail; activity only persists as latest snapshot, dedicated time-series store deferred to 6d if needed. (11) `dispatcher.md` placed at `prompts/dispatcher.md` siblng to `backlog.md` and `whatsnext.md` — **rejected at critic round 3 #10**; meta-prompts deserve their own subtree (`prompts/meta/`) for IA clarity and future expansion.
- **Constraints introduced**: **(a) Role catalog**: `backend/internal/roles/catalog.go` is hand-maintained `[]Role{...}` (no codegen); `Role.Category` ∈ {"role", "meta"}; `roles.All()` returns full set; `/api/roles` filters category="role"; `TestCatalogMatchesPromptDir` walks both `prompts/roles/*.md` and `prompts/meta/*.md`; PR adding a new role MUST edit both files. **(b) execution_role lifecycle**: writes go through `BacklogCandidateStore.UpdateExecutionRole(ctx, id, role, actor)` which does single-transaction validate → SELECT old → UPDATE → INSERT actor_audit; concurrent PATCH/apply use `BEGIN IMMEDIATE` (existing SQLite pattern). `set_by` ∈ {"", "operator", "router"}; `confidence` only set when `set_by="router"`. **(c) Apply API**: `mode=role_dispatch` requires non-empty `execution_role` in catalog → else 400; `mode=manual` ignores `execution_role`. (`mode=role_dispatch_auto` is deferred to Phase 6d per user pick B2 — see Alternatives §5.) **(d) Server-side claim enforcement**: `MarkTaskRoleNotFound` does `dispatch_status: queued → failed` atomic transition (NOT `running → failed`) plus single-tx audit row; non-applicable when task already leased. **(e) L0 safety**: per-role timeouts in catalog (code-reviewer=15min, test-writer=20min, api-contract-writer=30min, ui-scaffolder=45min, db-schema-designer=45min, backend-architect=90min, dispatcher=60min); `ANPM_DISPATCH_TIMEOUT` env override hierarchy: env>0 → env value, env=0 → disabled, env<0/unset → catalog → 30min fallback. SIGTERM→5s→SIGKILL escalation via `cmd.Cancel`+`cmd.WaitDelay`; adversarial test uses real subprocess with `signal.Ignore`. Output cap default 5 MB via `ANPM_DISPATCH_OUTPUT_MAX`; 0=disabled. JSON schema minimum: must contain `files []`; optional fields type-checked. **(f) Router**: dispatcher prompt is in `prompts/meta/dispatcher.md` (NOT under `roles/`) with `category: meta`; output validated for role_id ∈ catalog, confidence ∈ [0,1], reasoning length and control-char sanitization; alternatives all validated against catalog; `min_confidence` default 0.7 (reserved for Phase 6d auto-apply path); router output never trusted to be a valid catalog entry — Validation is a hard gate not a soft check. **(g) Activity model**: phases are exhaustive enum `idle/claiming_run/planning/claiming_task/dispatching/submitting` (no `routing` in 6c — that arrives in 6d alongside auto-apply); reporter uses **enqueue-not-overwrite** so rapid phase transitions all reach subscriber, only same-phase step changes coalesce within 500ms; in-memory Hub uses unbuffered subscriber channels with non-blocking send (slow client auto-drops, reconnect picks up via initial state); SSE includes 30s keepalive comments + `X-Accel-Buffering: no`; per-user concurrent SSE connections capped at 3 (503 above); DB persists latest snapshot only (Activity does NOT write to actor_audit per critic round 3 #8); idle activities retained 5 min before purge. **(h) Audit invariants**: every `execution_role` change writes to `actor_audit` in same transaction; `actor_kind` ∈ {"user", "router", "system", "connector"}; `rationale` stores router confidence + reasoning, or system change reason; cascade-delete with subject row. **(i) Operational constraint**: L0 boundary is the ONLY safety enforcement until L1 ships — operators MUST NOT expose role_dispatch to non-operator task submitters or untrusted task content; this is documented verbatim in `docs/operating-rules.md` and is non-negotiable. L1 (process-level jail via firejail/namespaces) is evaluated when Phase 6d opens; L2 (container/VM full isolation) requires one of three triggers fired: multi-tenant submitters, untrusted external repos, or compliance requirements. **(j) Phase 6d/7 trigger conditions**: recorded in `docs/phase6c-plan.md` §9; opening either phase without a documented trigger having fired is a scope-creep violation. **(k) PR ordering**: PR-1 first (already implemented); PR-2/3/4 sequential to avoid rebase cost (each later PR consumes earlier-PR types); PR-5 last (dogfood requires all four prior PRs).
- **Source**: `docs/phase6c-plan.md` v5.1 (post-critic-round-3, B2 + C1 拍板). Backed by dogfood-generated backlog candidates `bad629dc` (catalog SoT) and `fb040ce6` (safety boundary), both `approved` status as of 2026-04-25, plus design dialogues with the user that surfaced the catch-22, the LLM router request, the activity visibility requirement, and a critic round adversarially analyzing v5 that produced 14 findings (9 unilateral fixes adopted, B2 + C1 user-decided).

## 2026-04-25: Requirement discard, analysis filtering, and connector run-status badge [agent:application-implementer]

- **Context**: Three interconnected UX improvements: (1) Requirements with no applied tasks are permanently deletable (not just archivable); (2) `source=analysis` / `source=system` requirements should not appear in the sidebar or affect the isEmpty check; (3) The connector status badge should reflect when a run is actively leased or queued.
- **Decision**: (1) Added `DELETE /api/requirements/:id` endpoint. Backend enforces two guards: 409 if any `task_lineage` row references the requirement; 409 if any planning run is in an active state (queued/running/leased). Store adds `HasAppliedTasks`, `HasActiveRun`, and `Delete` methods to `RequirementStore`. Frontend adds `deleteRequirement` to `api/client.ts`. Hook adds `handleDiscardRequirement` which calls the endpoint and calls `loadAppliedLineageMeta` on success. (2) `PlanningTab` derives `sidebarRequirements = requirements.filter(r => r.source !== 'analysis' && r.source !== 'system')` and passes it to `RequirementQueue`; `isEmpty` now uses `sidebarRequirements.length === 0` so analysis-only projects still show the onboarding panel. Auto-select in the hook skips `source=analysis` and `source=system`. (3) `usePlanningWorkspaceData` derives `activeRunDispatchStatus` from `planningRuns`; `PlanningLauncher` receives it and shows "● Running job…" (amber) for `leased` and "⏳ Queued…" (blue) for `queued` instead of "● Online" (green). (4) `RequirementQueue` rework: `activeRequirements` and `archivedRequirements` split; archived requirements collapse into a toggle at the bottom; action button chooses between Discard (no lineage, handler provided, lineage data loaded) and Archive (has lineage or handler not provided); inline Discard confirmation overlay prevents accidental deletes. `loadAppliedLineageMeta` fetches `/projects/:id/task-lineage` on mount and after apply/discard to keep the lineage set current.
- **Alternatives considered**: (1) Soft-delete with a `deleted_at` column — rejected; unnecessary complexity for requirements that have never been applied to tasks. (2) Showing Discard always and catching the 409 error in UI — rejected; the backend guard is a safety net, not the primary UX signal. The lineage set gives real-time context. (3) Filtering `source=analysis` on the backend — rejected; the frontend needs the full list for other purposes (e.g. selecting a requirement after What's Next creates one). (4) Showing the archived requirements inline with an opacity — rejected; the collapsed section keeps the sidebar clean for operators with many archived requirements.
- **Constraints introduced**: (a) `requirementIdsWithAppliedTasks` starts as `undefined` (not yet loaded) and transitions to a `Set<string>` after the first fetch. `RequirementQueue` treats `undefined` as "show Archive" (safe fallback). (b) `DELETE /requirements/:id` is blocked if either guard fires; there is no force-delete bypass. (c) `activeRunDispatchStatus` is derived from `planningRuns` which are scoped to the currently selected requirement — the badge only shows the status of the selected requirement's active run, not globally. (d) The PlanningLauncher button label remains "Advanced" (matching existing tests T-6a-A2-1/A2-2/A2-3). A worktree-level inconsistency was found where the button had been changed to "Details" without updating tests; this was reverted in this pass.
- **Source**: User implementation request 2026-04-25. Tests: 143 frontend pass (23 files); `go build ./...` clean; `go test ./...` all packages pass; `make lint` clean.

## 2026-04-25: Planning tab — two-panel layout redesign (sidebar + workspace) [agent:application-implementer]

- **Context**: The Planning tab used a "vertical stack of cards" layout: AttentionRow + PlanningStepper + RequirementIntake (card) + RequirementQueue (card) + planning-workspace-card. This became hard to scan as projects grew; selecting a requirement required scrolling past the queue to reach the workspace.
- **Decision**: Replaced the vertical stack with two distinct states: (A) empty project → centered `planning-welcome-view` (max 640px, no card wrappers) rendered via a redesigned `WorkspaceOnboardingPanel`; (B) non-empty project → `planning-two-panel` grid (240px sidebar + flex main). The sidebar holds the requirement list (`RequirementQueue compact` mode — no card wrapper, no verbose header) and a "+ New" toggle that shows `RequirementIntake variant="inline"` (no card wrapper). The main panel shows the workspace components (PlanningLauncher, PlanningRunList, CandidateReviewPanel, AppliedLineage) when a requirement is selected, or an empty-state prompt + "Run What's Next" button when none is selected. `AttentionRow` and `PlanningStepper` are no longer rendered (their functionality moves to sidebar count badges and the main-panel empty state). `openDriftCount` and `onNavigateToDrift` props are kept in the interface for parent compatibility but are not rendered; `void` expressions suppress the lint error.
- **Alternatives considered**: (1) Keep vertical stack, add anchor links — rejected; doesn't solve the "too much scrolling" core problem. (2) Remove AttentionRow entirely vs. move to sidebar — moved count badges to sidebar footer which is always visible without scroll.
- **Constraints introduced**: (a) `RequirementQueue` now accepts `compact?: boolean`; when true omits the `.card` wrapper and header, renders a slim `.planning-req-compact` div. Existing non-compact usages are unaffected. (b) `RequirementIntake` now accepts `variant?: 'card' | 'inline'`; when `'inline'` renders inside `.planning-inline-form` with no header, no badge, and no toggle button — toggle is always externally controlled. (c) `WorkspaceOnboardingPanel` props interface is unchanged; `onWhatsnext` was never in the interface. (d) `PlanningTab` `openDriftCount` and `onNavigateToDrift` props remain in the interface for backward compatibility with `ProjectDetail.tsx`.
- **Source**: User design request 2026-04-25. Tests: 143 pass (23 files); `npm run build` and `make lint` clean.

## 2026-04-25: Planning launcher — migrate CLI selection from legacy account_bindings to per-connector cli_configs [agent:implementer]

- **Context**: Phase 6a moved CLI configuration from `account_bindings` (provider_id starting with `cli:`) to `local_connectors.metadata.cli_configs`. The planning launcher and its hook were still using the Phase 3 legacy path (`listAccountBindings` → filter `cli:*`), sending `account_binding_id` in planning run create payloads.
- **Decision**: `usePlanningWorkspaceData` now calls `listLocalConnectors` then `listConnectorCliConfigs` per non-revoked connector. Results are flattened into `CliConfigOption[]` (composite key `${connectorId}:${configId}`). Auto-selection prefers primary+online > primary > first. `handleCreatePlanningRun` and `handleRunWhatsnext` send `{ connector_id, cli_config_id }` instead of `{ account_binding_id }`. The `CliConfigOption` interface is exported from the hook for use by `PlanningLauncher.tsx` and its tests. `PlanningTab.tsx` `onRunCreated` callback now also calls `onReload()` after `onSelectLineage` so requirements refresh immediately after onboarding panel creates a run.
- **Alternatives considered**: Keeping `account_binding_id` as a fallback — rejected; the backend already supports `connector_id` + `cli_config_id` as the preferred path (per `CreatePlanningRunPayload` comment in types); using the legacy path creates confusing dual-path behavior.
- **Constraints introduced**: (a) `CliConfigOption.key` is the composite string `${connectorId}:${configId}`; splitting on `:` at send-time assumes neither ID contains a colon — this matches the UUID format used by the backend. (b) Connectors with `status === 'revoked'` are excluded; all other statuses (including `offline`) are included so the user can still select an offline connector's config. (c) `AccountBinding` import removed from `PlanningLauncher.tsx` and its test file. (d) `PlanningLauncher.test.tsx` updated: `makeBinding` helper replaced by `makeCliConfig`; test assertions updated for new label format `"{connectorLabel} — {configLabel} [{modelId}]"` and new link text "Set up CLIs in My Connector".
- **Source**: Dogfooding fix request 2026-04-25. Tests: 140 pass; `npm run build` clean.

## 2026-04-24: ModelSettingsHub UX redesign — Directions A/B/C/D [agent:implementer]

- **Context**: Follow-on UX pass on ModelSettingsHub post Phase-6a. Four directions in one rewrite: Direction A (live "Your current setup" banner), Direction B (simplified card titles and descriptions), Direction C (live connector status list in Option C card), Direction D (moved "Still unsure?" guidance into the banner, shown only when setupStatus === 'none').
- **Decision**: (A) All three data sources (getMeta, listAccountBindings, listLocalConnectors) load in one consolidated `useEffect` using `Promise.all`; per-online-connector CLI configs are fetched in a second parallel wave using `listConnectorCliConfigs`. `dataLoading` guards all loading states. `setupStatus` derives as `'option-c'` (online connector with ≥1 CLI config) > `'option-a'` (active non-cli: binding) > `'none'`. Banner uses inline `borderLeft` styling with green for A/C and warning-yellow for none — no new CSS class added. (B) Card `<h2>` titles changed to "API Key", "Local CLI (server-side)", "Your Machine's CLI". Descriptions shortened as specified. Option tags (A/B/C) unchanged. (C) Option C card replaces the static pointer with a live connector list: up to 3 non-revoked connectors shown with online/offline dot, status badge, and comma-joined CLI config labels; "+" overflow link points to /settings/connector. (D) Static "Still unsure?" `<div className="card">` at the bottom removed; equivalent content rendered inside the Direction A banner only when setupStatus === 'none'. (Tests) 7 tests in ModelSettingsHub.test.tsx — 2 preserved (T-6a-A6-1, T-6a-A6-2) plus 5 new (T-A6-banner-c, T-A6-banner-a, T-A6-banner-none, T-A6-connector-list, T-A6-d-no-footer). All 140 tests pass; lint clean.
- **Alternatives considered**: Using `getAllByText` in tests for elements appearing in both banner and card — rejected; tests instead use more specific text patterns (e.g. "Your setup: Option C") that only appear in the banner, keeping assertions unambiguous.
- **Constraints introduced**: (a) `setupStatus` check order is fixed — option-c always wins over option-a if both are true; this matches the intent that a live connector CLI session is the preferred path. (b) The `dataLoading` state consolidates what were previously separate `bindingsLoading` + two separate `useEffect` calls into a single effect; the `localModeState` for Option B mode detection remains in its own effect (getMeta is fast and independent). (c) `CliConfig` is imported from `api/client` (not `types/index.ts`) since that is where the type is defined.
- **Source**: User-requested UX redesign task 2026-04-24. Tests: 140 pass; make lint clean.

## 2026-04-24: Phase 6a Part 2 — Frontend slices A1–A6 [agent:implementer]

- **Context**: Phase 6a Part 2 adds onboarding UX improvements across six frontend slices: progressive disclosure in PlanningLauncher (A2), jargon tooltips via a shared `Jargon` component (A5), primary binding visibility in ModelSettingsHub (A6), a one-line workspace onboarding panel (A1), a requirement wizard modal (A3), and a demo seed banner (A4).
- **Decision**: (A2) PlanningLauncher wraps execution-mode selector, CLI binding selector, and model override in a collapsible "Advanced" section. Default state is collapsed; localStorage key `anpm_launcher_advanced_open` persists the toggle. The "Start Planning Run", "Run What's Next", and "Refresh Runs" buttons are always visible in `planning-run-actions`. "Run What's Next" was extracted from the local-connector-online section into the always-visible action bar. (A5) `Jargon.tsx` renders as `<abbr>` with underline-dotted decoration; `jargon.ts` holds the dictionary. Applied to 7 sites: PlanningLauncher (execution mode label), CandidateReviewPanel (backlog candidate heading + dispatch label), PlanningTab (requirement in empty-state), MyConnector (connector in h1), ConnectorCliConfigs (CLI config section header), AccountBindings (Primary badge x2), ModelSettingsHub (CLI config in Option C). (A6) ModelSettingsHub loads account bindings on mount, shows API bindings under Option A, CLI bindings under Option B (when local mode), and a connector pointer under Option C. Make-primary calls `updateAccountBinding` then re-fetches. (A1) `WorkspaceOnboardingPanel` renders when `requirements.length === 0`; posts createRequirement then getPlanningProviderOptions + createPlanningRun; calls `onRunCreated` on success. (A3) `RequirementWizardModal` adds audience + success_criteria fields; wired as "Refine" link in the onboarding panel. (A4) Demo seed banner shown when `planningRunsCount === 0` and localStorage dismiss key absent; calls `demoSeed` API.
- **Alternatives considered**: (A2) Hiding the "Run What's Next" button in advanced mode — rejected; spec explicitly says both action buttons always visible. (A5) Using a CSS `title` attribute directly rather than a component — rejected; a shared component ensures consistent styling and the dictionary is co-located. (A6) Showing all bindings in a flat list rather than per-option — rejected; per-option placement makes the relevance of each binding self-evident.
- **Constraints introduced**: (a) The `anpm_launcher_advanced_open` localStorage key must remain at the global (not per-project) scope — it is a UI preference, not per-project state. (b) `WorkspaceOnboardingPanel` only renders when `requirements.length === 0`; the demo banner requires additionally `planningRunsCount === 0`. (c) `audience` and `success_criteria` are added to `CreateRequirementPayload` in types; both are optional strings. (d) The `demoSeed` client function posts to `/projects/:id/demo-seed` — backend must return `{requirement_id, planning_run_id, message}` (per Phase 6a Part 2 backend decision).
- **Source**: Phase 6a Part 2 frontend slices. Tests: all 135 pass; `make lint` clean.

## 2026-04-24: Phase 6a Part 2 — AUDIENCE_LINE/SUCCESS_LINE prompt vars + demo-seed endpoint [agent:backend-architect]

- **Context**: Phase 6a Part 2 adds two backend slices. Slice A3 extends the `requirements` table with `audience` and `success_criteria` fields (migration 028) that are injected into the backlog planner prompt as `{{AUDIENCE_LINE}}` / `{{SUCCESS_LINE}}` template variables. Slice A4 adds a `POST /api/projects/:id/demo-seed` endpoint for onboarding new users on empty projects.
- **Decision**: (A3) Migration 028 adds `audience TEXT NOT NULL DEFAULT ''` and `success_criteria TEXT NOT NULL DEFAULT ''` to `requirements`. Both default to empty so all existing rows and golden fixtures are byte-identical to Phase 5. `buildAudienceLine` / `buildSuccessLine` helpers in `builtin_adapter.go` and equivalent Python code in `backlog_adapter.py` compute the pre-formatted lines (each includes its own leading `\n\n` so the template stays branch-free). The golden file is unchanged because both vars render to "" in the fixture. The `{{AUDIENCE_LINE}}{{SUCCESS_LINE}}` addition in `backlog.md` is appended inline after `{{REQUIREMENT}}` — when empty the output is byte-identical to Phase 5. (A4) `DemoSeed` uses the existing `RequirementStore.Create`, `PlanningRunStore.Create` (deterministic mode, no connector required), `BacklogCandidateStore.CreateDraftsForPlanningRun`, and `PlanningRunStore.Complete` — no new store methods needed. Returns 409 when the project already has requirements. Returns 201 with `{requirement_id, planning_run_id, message}` on success.
- **Alternatives considered**: (A3) Storing audience/success_criteria as JSONB or a separate wizard-context column — rejected; two named columns keep the fields individually indexable and the migration trivial on both SQLite and PostgreSQL. (A3) Injecting vars via a sub-template include — rejected; single-pass regex substitution is the established template engine (Phase 5 decision); adding branching logic there reopens the injection-safety discussion without benefit. (A4) Using `CompleteLocalConnectorRun` for the demo run — rejected; that method requires a leased connector_id and would fail without a real connector; `PlanningRunStore.Complete` is the correct path for in-process completion.
- **Constraints introduced**: (a) `AUDIENCE_LINE` and `SUCCESS_LINE` are now required template vars for the `backlog` prompt. Any caller that renders `backlog` without them will leave literal `{{AUDIENCE_LINE}}` / `{{SUCCESS_LINE}}` visible in the prompt (same "unknown var left as-is" contract from Phase 5). (b) The demo-seed 409 guard is requirement-count only; task count is not checked because `TaskStore` is not injected into `PlanningRunHandler`. This is intentional — any project with content will have requirements, and the guard is sufficient. (c) `DemoSeed` does NOT require auth on top of the normal route middleware — it relies on the same `RequireAuth` applied at the route group level; no special admin gate is needed for this endpoint.
- **Source**: Phase 6a Part 2 backend slices A3 and A4. Tests: `bash scripts/test-with-sqlite.sh` passes all packages; `python3 -m unittest adapters.test_prompt_loader` passes 7 tests.

## 2026-04-24: Phase 6a Part 1 — CLI configs move to connector metadata [agent:feature-planner]

- **Context**: Phase 3 Path B stored CLI bindings (`cli:claude` / `cli:codex`) in the user-level `account_bindings` table. Phase 4 P4-2 labelled that UI section "Server-side CLI Bindings (local-mode only)". That label was misleading: the same rows were ALSO used when a connector on the user's own machine runs the CLI. More importantly the user-level placement meant a user with two paired machines (Laptop with Claude, Workstation with Codex) could not express per-machine CLI + model preferences — the server just picked "any" online connector and hoped the CLI happened to be installed there with a matching path. Phase 6a's central redesign moves CLI configuration to the connector that runs it.
- **Decision**: CLI configs are now stored under `local_connectors.metadata.cli_configs[]` — a JSON array of `{ id, provider_id, cli_command, model_id, label, is_primary }` entries, one list per paired connector. Zero migration: the `metadata` JSONB column has existed since migration 025 (`cli_health`, `pending_cli_probe_requests`, `cli_probe_results` already live there); cli_configs is the next key in the same pattern. Five new authenticated endpoints scoped to `connector.user_id == caller.user.id` expose full CRUD + set-primary. `PlanningRun` create gains optional `connector_id` + `cli_config_id` fields (Step 0 in the `resolvePathBBinding` resolver); when present, the server snapshots the config from the connector's metadata onto `PlanningRun.connector_cli_info.binding_snapshot` — the connector daemon still receives the same `PlanningRunCliBindingPayload` shape it saw pre-Phase-6a, so no connector-protocol bump. Existing `cli:*` account_bindings continue to work as a legacy fallback; new bindings are no longer creatable from the AccountBindings UI (create flow removed), only Edit / Delete / Set-Primary on existing rows remain. A prominent banner points the user to My Connector for the new authoring surface.
- **Alternatives considered**: (1) Add nullable `connector_id` FK on `account_bindings` (Option B in design discussion) — rejected; keeping configs at user-level-with-optional-connector-affinity would have made the UI still feel like "bindings can be global or scoped" rather than the cleaner "configs belong to the connector that runs them". (2) Separate `connector_cli_configs` table (Option C) — rejected; the `metadata` JSONB pattern is already well-established for this kind of per-connector operational state, adds zero schema surface, and cascades on connector delete for free. (3) Auto-migrate existing `cli:*` account_bindings into each user's primary connector — rejected by owner; new users start fresh, existing users reconfigure manually under My Connector.
- **Constraints introduced**: (a) `cli_configs[]` length capped at `MaxCliConfigsPerConnector = 16` per connector — same rationale as `MaxAccountBindingConfiguredModels`. (b) "At least one primary when the list is non-empty" invariant enforced atomically: AddCliConfig auto-primaries the first entry; DeleteCliConfig promotes the first survivor when the deleted was primary; SetPrimary demotes the rest in the same transaction via `beginWriteTx` (same SQLite BEGIN IMMEDIATE pattern established in Phase 4 P4-4). (c) Planning run create must receive `connector_id` and `cli_config_id` TOGETHER; either alone returns 400. (d) `cli_config_id` is only valid for `execution_mode == "local_connector"`; setting it for any other mode returns 400. (e) The AccountBindings "CLI Bindings" section is frozen for Create; existing rows remain functional but should not grow. Part 2 will not re-enable Create. (f) No Phase 6b auto-dispatch code consumes `cli_configs[]` directly yet — the config surface is the authoring flow; dispatch still goes through the existing `PlanningRunCliBindingPayload` claim shape.
- **Source**: PR landing Part 1 of Phase 6a. Part 2 (UX-A1..A6) ships onboarding UX simplification separately. See `docs/phase6a-plan.md` for the slice-by-slice DoD and risk assessment.

## 2026-04-24: Phase 5 — prompt externalization + execution role library [agent:feature-planner]

- **Context**: Phase 4 shipped the gated-autonomy model (human picks which requirement → agent executes planning). Prompt text for the two planner adapters (`backlog`, `whatsnext`) was hardcoded as Python f-strings AND reimplemented as Go `strings.Builder` concatenations in `builtin_adapter.go` — two copies that had already drifted once (S5b). There was no "role library" — moving toward "agent writes backend / DB / API / UI" (the user-stated north star) had no prompt-schema home to grow into. A reference — GPT-Prompt-Hub — offered a clean role-prompt schema worth adopting. See `docs/phase5-plan.md` for the full problem statement.
- **Decision**: (1) All prompts become markdown files in `backend/internal/prompts/`. Go ships them via `//go:embed`; Python adapters read the same files from disk at `<repo-root>/backend/internal/prompts/<name>.md` (relative to `adapters/_prompt_loader.py`, with `ANPM_PROMPTS_DIR` env override for CI). (2) Template syntax is `{{VAR_NAME}}` — double brace, uppercase + digits + underscore, single-pass regex substitution. Values are NOT re-expanded (safety against injection via user-provided data); unknown variables are left as-is (mismatch is visible rather than silent). JSON schema examples in the prompt body use single braces — unambiguous. (3) Frontmatter follows the GPT-Prompt-Hub shape: `title / category / tags / version / use_case`. Both Go and Python strip it before substitution. (4) Six role prompts live under `backend/internal/prompts/roles/` — `backend-architect`, `ui-scaffolder`, `db-schema-designer`, `api-contract-writer`, `test-writer`, `code-reviewer` — as the Phase 5 library. They are NOT invoked in this PR; Phase 6 will wire the execution dispatcher that consumes them. (5) `backlog_candidates` gains a nullable `execution_role` TEXT column (migration 026) plus a matching `PATCH` payload field. Value is not catalog-enforced yet; Phase 6 owns that. (6) `POST /api/backlog-candidates/:id/apply` gains an optional `execution_mode` body field with enum `"manual" | "role_dispatch"`. Default is `"manual"` (preserves Phase 4 behaviour exactly). `"role_dispatch"` sets the created task's `source` to `"role_dispatch:<execution_role>"` (or bare `"role_dispatch"` when no role is set) so the audit trail distinguishes dispatch-earmarked tasks. Actual dispatch is Phase 6. (7) Parity between Python and Go rendering is pinned by `adapters/testdata/backlog_render_golden.txt` — both sides' tests render the same fixture and compare against the golden. Drift on either side fails CI.
- **Alternatives considered**: (1) Repo-root `prompts/` directory — rejected; Go's `//go:embed` cannot reach outside its containing package's directory, and we need the connector binary to ship prompts embedded. Build-time copy scripts or a separate Go module were rejected as heavier machinery than the one-line sentence "prompts live in `backend/internal/prompts/`" that the package README explains. (2) Real template engine (Go `text/template`, Jinja2) — rejected; double-brace Go templates would collide with JSON schema `{{` in the prompt body, and we do not need conditionals/loops — the caller pre-computes branch content into a single variable. Single-pass regex substitution is the minimal viable engine and behaves identically in both languages. (3) Catalog enforcement on `execution_role` PATCH — deferred to Phase 6 when the execution dispatcher lands. Today any string is accepted; bad values just mean Phase 6 will fail to dispatch. (4) `"role_dispatch"` actually dispatching today — rejected; this PR is the BREADCRUMB. Shipping fake dispatch would be worse than shipping nothing. The radio group is explicitly labelled "(coming in Phase 6)" and disabled.
- **Constraints introduced**: (a) Editing a prompt file REQUIRES updating both (i) the frontmatter `version` field and (ii) the golden fixtures (`adapters/testdata/backlog_render_golden.txt` + `whatsnext_render_golden.txt`) if the rendered output changed. The `UPDATE_PROMPT_GOLDEN=1` env var regenerates both via Python. Go reads the same files and must match — drift fails CI. (b) Prompts MUST stay side-effect-free text — no embedded tool invocations, no URLs resolved at runtime. Template substitution is the ONLY dynamic surface. (c) New role prompts MUST declare `category: role`, `role_id: <filename-stem>`, `version: 1`, and the shared output-contract variables documented in `backend/internal/prompts/roles/README.md`. (d) Phase 6 MUST introduce catalog enforcement for `execution_role` before shipping auto-dispatch; pre-dispatch, the field is a free-form string. (e) `execution_mode="role_dispatch"` is accepted today and writes a distinguished task `source`, but ALL downstream behaviour (what the dispatcher does, how failures are handled, how retries work) is a Phase 6 concern. This PR must not ship any code that consumes `source == "role_dispatch*"` — that is the dispatcher's job and would pre-empt Phase 6's design space. (f) `execution_role` is case-sensitive and must equal the role's filename stem (lowercased, hyphen-separated) — e.g. `"ui-scaffolder"`, not `"UI-Scaffolder"`. Phase 6 catalog enforcement will reject mismatched case. (g) **Phase 6 blocker**: the dispatcher that consumes role prompts MUST treat `{{TASK_DESCRIPTION}}` and `{{PROJECT_CONTEXT}}` as untrusted — they carry user-editable strings that flow into the agent's execution context. Sandboxing the resulting subprocess (filesystem scope, network scope, rollback semantics) is Phase 6's design responsibility and must not be quietly skipped. (h) Migration 026 requires SQLite ≥ 3.35 for the `.down.sql` path (`DROP COLUMN` was added in 2021-03). Documented in `docs/data-model.md`. Operators on older SQLite apply the forward migration fine; rollback requires a table-rebuild the down file does not implement.
- **Source**: PR landing this change. See `docs/phase5-plan.md` for slice-by-slice DoD matrices and `backend/internal/prompts/README.md` + `backend/internal/prompts/roles/README.md` for the runtime contract.

## 2026-04-24: Mandatory pre-PR verification pipeline [agent:feature-planner]

- **Context**: PR #21 shipped with green `make lint` and `npm test` locally but hit two CI failures (`TestProbeBindingApiKeyBindingReturns400` under both SQLite and PostgreSQL drivers) because the developer ran `go test` under a shell that had `APP_SETTINGS_MASTER_KEY` set while CI did not. Additionally Copilot's PR review caught issues that the critic subagent alone had not — gating on just the critic misses quality concerns that a first-read reviewer would flag. The loop needed a codified "all these checks must pass before the PR opens" surface so future agent sessions cannot accidentally skip a phase.
- **Decision**: Introduced a three-phase pre-PR gate in `docs/operating-rules.md` § "Pre-PR verification": **Phase 1** is `make pre-pr` (backed by `scripts/pre-pr-check.sh`) which runs the full CI pipeline locally including SQLite + PostgreSQL Go tests and the npm production build. **Phase 2** is the mandatory `critic` subagent review. **Phase 3** is `/security-review` + `risk-reviewer` for any diff that touches auth, protocols, schema, or state machines. All three phases must clear before `gh pr create`. `make pre-pr-fast` skips only the PostgreSQL suite and npm build for iteration; it is not a substitute for the full gate. The `Loop` summary in `AGENTS.md` is updated to name all three phases. Docs-only PRs skip Phase 1 stages 2-8 and Phase 3 but must pass Phase 1 stage 1 (`make lint-governance`); emergency hotfixes may waive Phase 3 with explicit owner approval.
- **Alternatives considered**: (1) Rely on the CI check-suite as the sole gate — rejected; CI runs AFTER the PR is open, which wastes reviewer attention on a red PR and makes a "no broken PRs" invariant unenforceable before the fact. (2) Make the pipeline a pre-push git hook — rejected as too aggressive; some branches are WIP that should reach the remote without running a full test matrix. The runner is explicit opt-in via `make pre-pr` so WIP pushes remain cheap. (3) Require `/ultrareview` on every PR — rejected; it is separately billed and cost-sensitive, reserved for PRs where the author wants a second opinion beyond critic + security-review.
- **Constraints introduced**: (a) `scripts/pre-pr-check.sh` is the single source of truth for Phase 1 stages. Any change to the CI workflow that adds or removes a check must be mirrored here; drift is a workflow violation. (b) The `critic` subagent MUST run after Phase 1 is green, not before — finding bugs in code that will not even compile wastes the critic's budget. (c) `/security-review` + `risk-reviewer` are not redundant; they catch orthogonal classes of findings (concrete CVE-style vs test/regression breadth). Any future "just run one of them" proposal requires a DECISIONS update.
- **Source**: This PR (Phase 4 follow-up addressing CI + Copilot review feedback).

## 2026-04-24: P4-4 probe pipeline — defensive bounds + strict heartbeat decode [agent:backend-architect]

- **Context**: Risk-reviewer pass on PR #21 surfaced three should-fix items on the P4-4 probe pipeline: (S-2) `ScrubCliProbesForBinding` used N sequential transactions so a connector added mid-scrub could be missed, (S-3) `pending_cli_probe_requests[]` had no per-connector cap so a runaway dedup regression could bloat metadata unboundedly, (S-5) `Heartbeat` decoded the entire request body strictly and returned HTTP 400 + offline-ing the connector if any single `cli_probe_results` entry was malformed.
- **Decision**: (S-2) All scrubs now commit in a single transaction via `txListConnectorsByUserForUpdate` — N round-trips collapse to 1 and all locks are held for the whole pass. Connectors added AFTER the SELECT are not scrubbed this round; the 24h `probeResultRetention` sweep picks them up. (S-3) Hard cap `maxPendingProbesPerConnector = 64` enforced in `EnqueueCliProbe`; overflow returns sentinel `ErrPendingProbeCapReached`, handler maps to HTTP 429. 64 entries ≈ 20 KB, well inside JSONB row-size limits, and dedup keeps normal usage at O(#CLI bindings) which is < 10 for typical operators. (S-5) Strict decode stays — a connector sending malformed JSON gets 400 and drops offline. Server now logs `heartbeat decode failed: remote=... err=...` before rejecting so an operator can tell "connector unreachable" from "connector buggy" in logs. Token hash is NOT logged (sensitive).
- **Alternatives considered**: (S-2) Switch to a single SQL UPDATE using `json_remove`/`jsonb_path_query` — rejected; SQLite + Postgres JSON surgery syntax diverges significantly and the one-transaction-many-updates pattern is already idiomatic in this store. (S-3) Drop oldest-pending silently instead of returning 429 — rejected; 429 is clearer for the UI to act on, and oldest-drop would mask a dedup regression rather than surface it. (S-5) Parse `cli_probe_results` as `[]json.RawMessage` and try each entry individually — rejected; the security boundary here is the T-S1 spoof guard, and per-entry tolerance would add complexity without fixing anything a well-behaved connector could cause.
- **Constraints introduced**: (a) `ScrubCliProbesForBinding` is atomic per-user; callers must not assume it also covers concurrently-paired connectors — those rely on the 24h GC. (b) `maxPendingProbesPerConnector` is a hard cap; raising it is a DECISIONS-level change because JSONB row size is not per-entry bounded. (c) Heartbeat remains strict-by-design; any future connector-protocol change that adds optional fields MUST be additive (use `omitempty`) or bump a protocol_version so old connectors can be blocked cleanly rather than silently dropping decode errors.
- **Source**: Phase 4 fix-up pass. See `docs/phase4-plan.md` risk-reviewer follow-up.

## 2026-04-24: ProjectDetail primary/secondary tab split + Settings gear [agent:feature-planner]

- **Context**: `frontend/src/pages/ProjectDetail.tsx` exposed seven co-equal tabs (Overview, Workspace, Tasks, Documents, Drift, Activity, Settings). First-time operators could not tell which surface was the main entry point; Settings in particular competed for attention equally with the default Workspace surface despite being rarely opened.
- **Decision**: ProjectDetail's rail is now a two-tier structure. Primary rail (always visible, in order): Workspace · Overview · Tasks · Documents. Secondary group under a "More ▾" popover: Drift · Activity. Settings moves to a gear icon in the project header (right of "Sync Now"). The "More ▾" button shows an aggregated count/dot when demoted tabs carry actionable state (open drift signals, recent agent runs) so demotion never blinds the operator. All `?tab=<slug>` deep links continue to resolve; routing and default-tab (Workspace, per 2026-04-22 Phase 2 S5 ADR) are unchanged.
- **Alternatives considered**: (1) Keep Drift in the primary rail and demote only Activity — rejected because Drift already has a count badge in its rail button; demoting it does not lose signal. (2) Also demote Documents — rejected because document-centric projects use it as often as Tasks. (3) Remove Settings entirely and move per-project configuration into a right-hand slide-out panel — rejected; the gear-icon affordance is cheaper, reuses the existing tab machinery, and keeps Settings URL-addressable for docs/screenshots. (4) Keep seven tabs but add an onboarding tooltip — rejected as a band-aid over a real IA problem.
- **Constraints introduced**: (a) The primary rail's four tabs are a stable set. Any future new ProjectDetail feature that looks like a "primary" surface should demote one of the existing four, not grow the rail to five. (b) `More ▾` popover content must render a count/dot indicator for any contained tab whose demotion would otherwise hide actionable state (the test case T-P4-1-8 encodes this invariant). (c) Settings gear icon uses `aria-label="Project settings"` and active-tab styling — any refactor of the page-header actions block must preserve both.
- **Source**: Phase 4 P4-1. See `docs/phase4-plan.md` §5 P4-1 for the slice's DoD and test matrix.

## 2026-04-24: Model Settings hub page at `/settings/models-hub` [agent:feature-planner]

- **Context**: The three execution paths for planning runs (hosted API via server, server-side CLI subprocess, local-machine CLI via connector) each had their own settings page (`/settings/account-bindings`, same page for CLI bindings, `/settings/connector`) plus an admin-only shared-key page (`/settings/models`). The account dropdown listed them as peers; a first-time operator had no single place that framed the three options.
- **Decision**: A new landing page `/settings/models-hub` (`ModelSettingsHub.tsx`) renders three cards (Option A / B / C) each explaining one execution path and linking to the relevant existing settings page. The account dropdown's top entry is now "Model Settings" pointing at the hub; the three targets remain directly linkable from the dropdown below a separator so existing bookmarks still work. Option B is disabled with an explanatory note when the server is not in local mode (avoids a false-promise link). AccountBindings' CLI Bindings H2 is renamed to "Server-side CLI Bindings (local-mode only)" and MyConnector's subtitle gains one disambiguating sentence.
- **Alternatives considered**: (1) Move all settings under a single mega-page — rejected; the three pages already work well for operators who know which one they need; the hub is purely a discovery aid. (2) Rename `/settings/models` (currently admin-only shared key) to `/settings/models/shared-key` and put the hub at `/settings/models` — rejected to avoid URL churn in existing admin docs/bookmarks. (3) Put the disambiguation in AccountBindings alone without a dedicated hub — rejected; AccountBindings is "Option A + B" and has no natural place to mention Option C (My Connector). (4) Require admin role for hub visibility — rejected; the hub is purely informational, useful to any logged-in user.
- **Constraints introduced**: (a) `/settings/models` (admin shared key) stays at its existing path; dropdown label is now "Shared API Key (admin)" for clarity. (b) Hub page MUST NOT duplicate form inputs from the three target pages — it is a pointer, not a replacement. Future settings additions that belong to one of the three paths should go into that path's page, not the hub. (c) Option B's disabled-when-not-local-mode branch is load-bearing: removing it would send server-mode users to a form that 403s.
- **Source**: Phase 4 P4-2. See `docs/phase4-plan.md` §5 P4-2.

## 2026-04-24: Connector CLI-binding model probe via heartbeat round-trip [agent:backend-architect]

- **Context**: Phase 3 S5b added a `<cli_command> --version` liveness probe that updates `metadata.cli_last_healthy_at`. That confirms the CLI is on disk and executable but does not exercise the model — operators had no way to click a button and verify "will this CLI+model actually produce a completion right now?" before starting a real planning run.
- **Decision**: A CLI-binding probe is an ephemeral request stored in `local_connectors.metadata.pending_cli_probe_requests[]` (server-written) and its outcome in `local_connectors.metadata.cli_probe_results.<probe_id>` (connector-written via the next heartbeat). No new table; no PlanningRun row; no new migration. The server exposes two endpoints: `POST /api/me/local-connectors/:id/probe-binding` enqueues a probe (dedup by `binding_id`: a second click while one is in-flight returns the same `probe_id`), and `GET /api/me/local-connectors/:id/probe-binding/:probe_id` returns the stored result or `{status: "pending"}`. The connector runs each pending probe in a worker goroutine so heartbeat cadence is never blocked; the probe itself reuses `resolveBuiltinCLI` + `invokeBuiltinCLI` with a fixed one-line prompt (`"Respond with exactly the single word: ok"`) and a 60s timeout. Probe prompt text is hardcoded on the connector side — the server never sends free-text instructions over the wire, preserving the §D7 constraint. Completed results are retained for 24h (GC'd at enqueue + heartbeat time) and scrubbed immediately on binding delete.
- **Alternatives considered**: (1) Enqueue a real `PlanningRun` with an `is_probe=true` flag and reuse the entire claim→execute→submit path — rejected; pollutes planning history, requires a DB migration, and the probe prompt would still need a special-case branch inside the adapter. (2) Send the probe prompt from the server so the admin can customise it — rejected per §D7 (compromised server-side control text is out of scope, but avoiding free-text-over-wire is the principle). (3) Use a dedicated `connector_probes` table instead of `metadata` — rejected; probes are ephemeral (24h), the metadata JSONB already round-trips every heartbeat, and a table would add a migration and N new queries for no operational benefit at the expected scale (tens of probes per user per day). (4) Use SSE from server→frontend instead of polling — rejected for the initial slice; 2s polling with a 30s cap is simple, observable, and cheap enough for a manual operator action.
- **Constraints introduced**: (a) `metadata.pending_cli_probe_requests[]` entries MUST be dedup'd by `binding_id` at enqueue time; a second click for the same binding returns the existing `probe_id`. (b) Connector-side probe worker runs probes **serially** (one at a time) to keep load predictable on a shared subscription CLI; parallelism is a future optimisation, not this slice's goal. (c) The probe worker goroutine MUST NOT block the heartbeat goroutine — results are accumulated in `Service.probeResultsOut` and drained into the next heartbeat request body. (d) On binding delete, `AccountBindingHandler.Delete` calls `LocalConnectorStore.ScrubCliProbesForBinding` best-effort; a scrub failure does not un-do the delete. (e) `ExecuteProbe` treats any non-empty stdout as success — it does NOT require the CLI to emit structured JSON. This is deliberate: the goal is to verify CLI+model liveness, not adapter-contract conformance.
- **Source**: Phase 4 P4-4. See `docs/phase4-plan.md` §5 P4-4 for the full slice.

## 2026-04-24: Phase 3 probe `updated_at` bump stays [agent:feature-planner]

- **Context**: Phase 3 open question (`docs/phase3-plan.md` §8 Q3) left undecided whether `RecordProbe` should bump `account_bindings.updated_at`. Bumping keeps `updated_at` as "last DB write"; not bumping preserves `updated_at` as a proxy for "last user edit."
- **Decision**: Keep the bump. `last_probe_at` is the authoritative probe timestamp; `updated_at` tracks last DB write and is allowed to be noisy. If a future UX feature needs a "last edited by user" signal it must add a dedicated column rather than re-derive it from `updated_at`.
- **Alternatives considered**: Dropping the bump — rejected because two other code paths (primary-demotion, probe GC on connector delete) also touch `updated_at` without user action; making probe the only "quiet" writer would be inconsistent.
- **Constraints introduced**: Any new "last user edit" feature MUST add its own column; do not overload `updated_at`.
- **Source**: Phase 4 P4-5. Resolves `docs/phase3-plan.md` §8 Q3.

## 2026-04-24: Phase 3 by-evidence query strategy [agent:feature-planner]

- **Context**: Phase 3 open question (`docs/phase3-plan.md` §8 Q1) left Go-layer JSON scan vs dual-dialect SQL undecided for the `GET /api/projects/:id/backlog-candidates/by-evidence` endpoint.
- **Decision**: Go-layer scan is the default. The endpoint iterates the project's candidates and filters `evidence_detail` in Go rather than pushing the predicate to SQL. Revisit when a single project routinely exceeds ~500 backlog candidates (observed query time > 50 ms).
- **Alternatives considered**: Dual-dialect SQL (`@>` in Postgres, `json_each` in SQLite) — rejected for this slice; the SQLite side would be un-indexed anyway and the complexity cost exceeds the benefit at the current scale.
- **Constraints introduced**: Benchmark before switching to SQL-side JSON queries; the revisit trigger is candidate count, not query count.
- **Source**: Phase 4 P4-5. Resolves `docs/phase3-plan.md` §8 Q1 and Q4.

## 2026-04-24: Phase 3 pending-review batching threshold [agent:feature-planner]

- **Context**: Phase 3 open question (`docs/phase3-plan.md` §8 Q2) deferred the per-project vs batch-endpoint decision for the Dashboard pending-review count.
- **Decision**: Per-project fetch (`GET /api/projects/:id/pending-review-count`) stays as the default. Introduce a batch `GET /api/dashboard/attention` endpoint only when a user routinely lists more than 20 projects. The trigger is project count, not request count.
- **Alternatives considered**: Eagerly build a batch endpoint "just in case" — rejected as premature; the per-project path is already shipped and working, and the batch endpoint would need its own authorisation model (which subset of projects does the user see?) that the per-project path already inherits from project auth.
- **Constraints introduced**: `Dashboard.tsx` fetch fan-out is bounded by the project list; large accounts cross the 20-project threshold should surface the need for a batch endpoint via UX latency rather than pre-optimised code.
- **Source**: Phase 4 P4-5. Resolves `docs/phase3-plan.md` §8 Q2.

## 2026-04-23: Built-in Go adapter for anpm-connector; zero-flag `serve` [agent:backend-architect]

- **Context**: The local connector binary required `--adapter-command python3 --adapter-arg adapters/backlog_adapter.py` at every startup. This blocked the zero-configuration use-case (`./bin/anpm-connector serve` with no flags) which is the natural entry point for users who have Claude or Codex authenticated via CLI subscription.
- **Decision**: (1) New file `backend/internal/connector/builtin_adapter.go` exports `ExecuteBuiltin(ctx, ExecJSONInput) LocalConnectorSubmitRunResultRequest`. It resolves the CLI via the same D4 precedence as the Python reference adapter: `cli_selection` from the claim response first, then PATH lookup of `claude`, then `codex`. It builds prompts by porting `_build_prompt` from both `backlog_adapter.py` (adapter_type `"backlog"` or empty) and `whatsnext_adapter.py` (adapter_type `"whatsnext"`). For Claude it uses `exec.CommandContext` with `-p`; for Codex it uses `github.com/creack/pty` to satisfy Codex's `stdin.isTTY` check. JSON extraction uses the same three-strategy fallback as the Python adapters (fenced block, first `{...}`, direct parse). Candidate normalisation clamps scores to `[0,1]`, truncates title to 120 runes, fills missing rank sequentially. (2) `Service.RunOnce` dispatches to `ExecuteBuiltin` when `State.Adapter.Command == ""` and to `ExecuteExecJSON` otherwise — the exec-json subprocess path is fully preserved. (3) `runServe` calls `loadRuntimeState(args, false)` instead of `true`; the connector no longer refuses to start without `--adapter-command`. (4) `emitAdapterDiagnostics` prints an info message (`"using built-in adapter; CLI and adapter type are read from each run's configuration"`) instead of a warning when no adapter command is configured. (5) `runDoctor` note updated to say `serve` will use the built-in adapter rather than telling the user to configure `--adapter-command` before running serve. (6) Tests in `builtin_adapter_test.go` cover `extractJSONFromOutput` (fenced, raw braces, ANSI strip, direct parse, no-JSON error), `buildBuiltinPrompt` (backlog / whatsnext / nil context / nil requirement / with planning context), `normalizeBuiltinCandidates` (clamp, truncate, fill rank, skip empty title, maxCandidates cap), `resolveBuiltinCLI` (nil selection, CLI command, provider prefix, model override), `stripANSI`, and `ExecuteBuiltin` integration with a shell stub (success, non-zero exit, no PATH fallback, model override).
- **Alternatives considered**: (1) Bundle the Python adapter at build time using `go:embed` — rejected; embedding a Python interpreter path adds runtime brittleness and keeps the Python dependency surface on PATH. (2) Use `os/exec` PTY via `syscall.SysProcAttr{Setsid:true}` instead of `creack/pty` — rejected; manual PTY allocation is 60+ lines of platform-specific syscall code that `creack/pty` handles correctly across Linux/macOS. (3) Always run both adapters (subprocess then builtin as fallback) — rejected; would hide misconfigured subprocess adapters and produce ambiguous diagnostics. (4) Remove the Python adapters — rejected; they remain the reference implementation and the advanced-user escape hatch.
- **Constraints introduced**: `ExecuteBuiltin` MUST NOT call `ExecJSONAdapterConfig.Normalized()` — that function rejects empty `Command` and would produce a confusing error on the built-in path. The dispatch branch in `Service.RunOnce` (command == "" → builtin, command != "" → exec-json) is the single gate; callers must not introduce a second dispatch. The PTY path for Codex is Linux/macOS only; Windows is not supported and `pty.Start` will return an error that surfaces as a clean failure message. `creack/pty v1.1.24` is now a direct dependency of the `backend` module.
- **Source**: This commit. Tests at `backend/internal/connector/builtin_adapter_test.go` (27 new tests, all passing). `go test ./...` passes.

## 2026-04-23: Path B — per-run CLI binding picker in PlanningLauncher; MyConnector adapter config removed [agent:application-implementer]

- **Context**: Before this change, the PlanningLauncher exposed adapter-type radio buttons (`backlog` / `whatsnext`) and a free-text model-override input for local-connector runs. The connector binary now has a built-in adapter that selects adapter type automatically based on the run type, and CLI selection (Claude vs Codex, model) is controlled by `AccountBinding` records where `provider_id.startsWith('cli:')`. The serve command has simplified to `./bin/anpm-connector serve` with no flags.
- **Decision**: (1) `CreatePlanningRunPayload` in `types/index.ts` gains `account_binding_id?: string`. (2) `usePlanningWorkspaceData` drops `localAdapterType`/`localModelOverride` state and handlers; adds `cliBindings: AccountBinding[]`, `cliBindingsLoading`, `selectedCliBindingId`, `onCliBindingChange`. A new `loadCliBindings()` fetches `listAccountBindings()` and filters to `provider_id.startsWith('cli:') && is_active`; it fires when `planningSelectedExecutionMode === 'local_connector'`. `handleCreatePlanningRun` always sends `adapter_type: 'backlog'` for local-connector runs; `handleRunWhatsnext` always sends `adapter_type: 'whatsnext'`. Both pass `account_binding_id: selectedCliBindingId` when non-null. (3) `PlanningLauncher` replaces the adapter-type fieldset + model-override input with a CLI binding `<select>` (loading state, empty state with link to `/account-bindings`, populated state showing `label [model_id] (primary)`). An informational `<small>` note explains that adapter type is chosen automatically. (4) `MyConnector.tsx` removes `AdapterConfig`, `adapterModels`, `loadAdapterConfig/saveAdapterConfig`, `updateAdapterConfig`, `loadAdapterModels`, the adapter config panel JSX, the `datalist` elements, and `getAdapterModels` from imports. `ServeCommand` becomes a zero-prop component that shows `./bin/anpm-connector serve`. Run-stats links now always show both Claude and OpenAI usage links.
- **Alternatives considered**: (1) Keep model-override input in PlanningLauncher alongside the binding select — rejected; model is part of the binding's `model_id` and showing both would create conflicting controls. (2) Fetch CLI bindings once at hook mount unconditionally — rejected; CLI bindings are only relevant when `execution_mode === 'local_connector'`, fetching unconditionally wastes a network call for server-provider-only users. (3) Use `useCallback` to memoize `loadCliBindings` — considered but not needed; the `react-hooks/exhaustive-deps` rule is not enabled in this project and the dep array `[planningSelectedExecutionMode]` correctly controls when re-fetch fires.
- **Constraints introduced**: `adapter_type` is NOT user-selectable in the UI. "Start Planning Run" always uses `adapter_type: 'backlog'`; "Run What's Next" always uses `adapter_type: 'whatsnext'`. This is fixed at the UI layer. If the connector binary ever needs a different adapter type for requirement runs, a separate UI decision is required. The `loadCliBindings` effect fires on every transition into `local_connector` mode; if the user toggles back and forth it re-fetches each time (acceptable given low cost and need for fresh data). `ServeCommand` in `MyConnector.tsx` no longer accepts a `projectPath` prop.
- **Source**: This change (Path B frontend S4 + MyConnector simplification). Tests: `PlanningLauncher.test.tsx` (10 tests, 3 new). `make lint` and `npm test` both pass.

## 2026-04-23: Path B Slice S5a — error_kind catalog + remediation hint [agent:backend-architect]

- **Context**: Planning runs dispatched via the local connector had no structured failure classification. When the adapter exited because of a session expiry, rate limit, context overflow, or adapter timeout, the UI showed only a raw error string. Design §5 D7 specifies a phishing-mitigation constraint: the adapter sends a structured `error_kind` enum (not a free-text hint), the server validates it against an allowlist, and the server looks up the human-readable remediation from a static Go map. This keeps all hint text under server control so a compromised adapter cannot inject arbitrary instructions into the UI.
- **Decision**: (1) `LocalConnectorSubmitRunResultRequest` gains an optional `error_kind string` field. (2) `PlanningRunCliInfo` gains `RemediationHint string` alongside the pre-existing `ErrorKind string`. (3) Five error-kind constants (`unknown`, `session_expired`, `rate_limited`, `context_overflow`, `adapter_timeout`), the allowlist map `AllowedErrorKinds`, and the catalog map `ErrorKindRemediations` are defined in `models/requirement.go`. `ErrorKindUnknown` is the only allowlist member without a catalog entry (no hint for an unclassified error). (4) `PlanningRunStore.MarkErrorKind(id, errorKind, remediationHint string) error` follows the same read-merge-write envelope pattern as `MarkDispatchWarning`. (5) In the `!req.Success` branch of `HandleSubmitRunResult`, after `FailLocalConnectorRun` succeeds, the handler normalises `error_kind` (missing/unknown-enum → `"unknown"`) and calls `MarkErrorKind` best-effort. (6) Frontend: `PlanningRun.connector_cli_info` type in `types/index.ts` is updated to the full S2-envelope shape with `error_kind` and `remediation_hint`. `CandidateReviewPanel.tsx` fixes the CLI invocation display for S2 envelope rows and adds a failure banner that renders `connector_cli_info.remediation_hint` when the run is `returned`/`failed` with a non-`unknown` `error_kind`. The hint is read-only (no input or textarea).
- **Alternatives considered**: (1) Adapter supplies free-text hint — rejected per design §5 D7; a compromised adapter could inject arbitrary instructions into the UI. (2) Server computes the hint at read time rather than persisting it — rejected; the static catalog is small and the read path is simpler with a persisted value. (3) Separate `error_catalog` table — rejected; the catalog is static Go code and adding a table adds a migration for no benefit.
- **Constraints introduced**: `AllowedErrorKinds` is the server-side gate. Any new `error_kind` value the adapter wants to send MUST be added to `AllowedErrorKinds` AND `ErrorKindRemediations` (for non-`unknown` kinds) in the same PR, with a paired DECISIONS entry. The T-S5a-5 test enforces the catalog-completeness contract at CI time. `remediation_hint` in `connector_cli_info` MUST always come from the server-side catalog — any attempt to store adapter-supplied free text in this field is prohibited.
- **Source**: This PR (S5a of Path B). Design at `docs/path-b-subscription-cli-bridge-design.md` §5 D7, §8 S5a DoD. Test matrix T-S5a-1 through T-S5a-6 in `backend/internal/handlers/planning_runs_path_b_test.go` and `frontend/src/pages/ProjectDetail/planning/CandidateReviewPanel.test.tsx`.

## 2026-04-23: Path B Slice S1 — `account_bindings` extended for CLI bindings, gated to local mode [agent:backend-architect]

- **Context**: The Path B design (`docs/path-b-subscription-cli-bridge-design.md`, design v2 dated 2026-04-23) gates implementation slices S1–S5. S1 is the data-model slice: extend `account_bindings` to describe a CLI binding (`provider_id LIKE 'cli:%'`), enforce shape + size + sanity validation, gate CLI bindings to local mode (D8), and ship the test matrix (T-S1-1 through T-S1-10). The existing `account_bindings` table from migration 014 + the active-uniqueness index from migration 015 are reused; no new table.
- **Decision**: Migration `021_account_bindings_cli_extensions.sql` adds two columns (`cli_command`, `is_primary`) and one partial unique index `idx_account_bindings_primary_unique` keyed on `(user_id, CASE WHEN provider_id LIKE 'cli:%' THEN 'cli' ELSE 'api' END)`. The `account_bindings.go` model gains `CliCommand` and `IsPrimary`. The handler (`AccountBindingHandler`) gains a `WithLocalMode(bool)` builder; the Create / Update paths reject `cli:*` providers with 403 when `LocalMode=false`. The store enforces a server-side allowlist (`openai-compatible`, `cli:claude`, `cli:codex`), shape constraints for `cli:*` (empty `base_url`, empty `api_key`, required `model_id`), a `cli_command` regex sanity check (`^/[A-Za-z0-9_./\-]+$`), a `configured_models` cap (16 entries × 64 chars), and atomic primary-binding demotion within the same TX as INSERT/UPDATE. Sentinel errors map to proper HTTP status codes: validation → 400, D8 gate → 403, not-found → 404, uniqueness conflicts (`active_unique`, `primary_unique`, label) → 409. The migration runner now skips `*.down.sql` siblings so the design §13 dev-only rollback companions can ship without being treated as forward migrations. `database.IsUniqueViolation` was broadened to recognise `pq` error formats that surface `(23505)` without the `SQLSTATE` prefix.
- **Alternatives considered**: (1) New `cli_bindings` table — rejected per design §5 D1 (90% schema overlap; would duplicate per-user uniqueness, CRUD endpoints, credential-mode resolution). (2) Allow `cli:*` in server mode behind a regex allowlist alone — rejected per design §5 D8; the regex is theatrical (symlinks, interpreter binaries, suid binaries all pass). Server-mode CLI execution needs a separate design with admin-managed binary allowlists, interpreter denylists, and probably containerized adapter execution. (3) Treat `is_primary` as derived from `updated_at` ordering — rejected per design §5 D2; concurrent runs touch `updated_at`, failed runs don't update, so the ordering races. Explicit `is_primary BOOLEAN` plus a partial unique index removes the race. (4) Move the down-migration files outside `backend/db/migrations/` — rejected; the design §13 sibling-file pattern is explicit, and a four-line filter in the runner is cheaper than relocating a convention.
- **Constraints introduced**:
  - Adding any new `cli:*` `provider_id` to `models.AllowedAccountBindingProviderIDs` requires a paired DECISIONS entry (design §9 R16).
  - The CASE expression in `idx_account_bindings_primary_unique` and the Go-side `primaryNamespaceForProvider` helper MUST be kept in lockstep — diverging definitions silently re-introduce multi-primary bugs.
  - `cli_command` server-side validation is sanity-only. The connector enforces real hardening (`realpath` resolution, allowed-roots, interpreter blocklist, setuid rejection) at probe / invocation time per design §10 — that work lands in S2 / S5b, not S1.
  - The migration runner now skips files whose name ends in `.down.sql`. New down-migration files MUST follow this naming convention or they will be applied as forward migrations.
  - `database.IsUniqueViolation` is now broader; callers that need constraint-name granularity should still inspect `pq.Error.Constraint` directly on the Postgres path.
- **Source**: This PR (S1 of Path B). Design at `docs/path-b-subscription-cli-bridge-design.md` §5 (D1, D2, D7, D8), §6.1, §6.2, §8 S1 DoD, §10, §13. Test matrix T-S1-1 through T-S1-10 codified in `backend/internal/handlers/account_bindings_test.go`. T-S1-10 is currently a Skip placeholder per the design's reduction clause: this repo has no established CI rollback fixture. Both `bash scripts/test-with-sqlite.sh` and `bash scripts/test-with-postgres.sh` pass; `make lint-governance` shows only the pre-existing accepted "Scope: decision tracking" warning.

## 2026-04-23: Tab/panel components migrated under `pages/ProjectDetail/`; Mistral added as a connection preset; `mvp-scope.md` repurposed as a scope pointer [agent:application-implementer]

- **Context**: Three discrete cleanup items were bundled into one PR. (a) The 2026-04-22 Planning Workspace ADR explicitly grandfathered the existing `frontend/src/components/*Tab.tsx` files because the Phase 2 sibling rule (`new components MUST land under frontend/src/pages/ProjectDetail/planning/`) only applied to *new* features. The grandfathering note also said: "They can migrate in a later cleanup pass." (b) `docs/mvp-scope.md` had been carrying a HISTORICAL banner since the 2026-04-22 docs sync, but the file body still listed Phase 1 in/out checklists that were no longer accurate; 13 references across `AGENTS.md`, `.claude/agents/*`, `.github/copilot-instructions.md`, `docs/rules-quickstart.md`, `docs/agent-playbook.md`, and `docs/context-strategy.md` still pointed agents to it for "is this in current phase?" decisions. (c) The owner only has a Mistral API key available for testing the OpenAI-compatible planning provider; the existing Connection Template grid had presets for Ollama and LM Studio (local) plus a generic "Custom" entry, but no one-click Mistral preset.
- **Decision**:
  - (a) All eight tab/panel components plus `PlanningStepper.tsx` were `git mv`'d from `frontend/src/components/` into `frontend/src/pages/ProjectDetail/` (flat, sibling to `ProjectDetail.tsx` and to the existing `planning/` subdirectory). Internal import paths (`../types`, `../api/client`, `../utils/...`) were updated to `../../X` to match the new depth; `vi.mock('../api/client', ...)` calls in tests were updated identically. The now-empty `frontend/src/components/` directory was removed. `ProjectDetail.tsx`'s eight tab imports were rewritten from `../components/X` to `./ProjectDetail/X`. The README project-tree was updated to reflect the new shape.
  - (b) `docs/mvp-scope.md` was rewritten to a ~30-line "Scope Pointer" document that names the current authoritative sources (`docs/product-blueprint.md`, `DECISIONS.md`, `docs/api-surface.md`, `docs/data-model.md`, `ARCHITECTURE.md`) and lists three out-of-scope shortcuts (non-goals contradiction, active DECISIONS supersession, net-new top-level subsystem). The 13 inbound references were kept as-is because the file is now a deliberate redirect surface — agents that follow them land on a useful index, not on a misleading checklist. `docs/product-blueprint.md` was de-circularized (the line "See `docs/mvp-scope.md` for the detailed feature list" was rewritten to point at the current sources directly).
  - (c) `frontend/src/utils/planningConnectionPresets.ts` gained one preset entry `mistral-cloud` with `baseURL: https://api.mistral.ai/v1`, default model `mistral-small-latest`, configured-models placeholder listing six common Mistral models, `apiKeyMode: 'optional'`, `advancedOnly: true`. Same render path as the existing Custom preset — user clicks "Show Advanced Fields" once to see the API key input. Unit tests were added at `planningConnectionPresets.test.ts` (5 cases covering id list, lookup, fallback, baseURL inference with case/trailing-slash, unmatched URL fallback). The preset file is shared between `ModelSettings.tsx` (admin shared key) and `AccountBindings.tsx` (per-user key) so both surfaces pick up Mistral automatically.
- **Alternatives considered**: (a-1) Subgroup the migrated tabs under `pages/ProjectDetail/tabs/` — rejected; the existing `planning/` subdirectory is a feature subgroup (seven tightly-related siblings), but the eight tabs are conceptually peers of `ProjectDetail.tsx` itself. Flat layout matches what an operator looking for "the documents tab component" expects to find. (a-2) Also migrate `frontend/src/pages/<other>` siblings (`Dashboard.tsx`, `ProjectList.tsx`, `Login.tsx`) into the same restructure — rejected as scope creep; only the `ProjectDetail` family was grandfathered, only it gets the cleanup pass. (b-1) Delete `docs/mvp-scope.md` outright and update all 13 references — rejected as 10× the blast radius for the same goal; the file's address is now part of the agent-bootstrap contract and a deliberate redirect is cheaper than rewriting every consumer. (b-2) Run a second `DECISIONS.md` archival pass in this PR — rejected after closer review; on a per-entry basis the remaining entries are still load-bearing (supersession chains for the SQLite ↔ PostgreSQL ↔ Dual-runtime trail; planning context.v1 sanitizer scope; credential mode resolution; local connector context byte cap). The 30 KB threshold is a guideline, not a hard limit, and trimming load-bearing entries to satisfy a guideline would lose project memory. (c-1) Add Mistral as a backend provider implementation rather than a frontend preset — rejected; the existing OpenAI-compatible provider already speaks Mistral's API verbatim (auth header, request/response shape, JSON content extraction, fenced-code stripping). The only gap was UX discoverability. (c-2) Use `apiKeyMode: 'required'` to gate save when the field is empty — rejected as scope creep; would require expanding the preset type and adding new render logic. The user learns from the 401 response, which is acceptable for a self-hosted single-operator setup.
- **Constraints introduced**:
  - The 2026-04-22 grandfathering of `frontend/src/components/*Tab.tsx` is now resolved. New tab/panel-style components for `ProjectDetail` MUST land under `frontend/src/pages/ProjectDetail/` (flat) or under a feature subdirectory like `planning/` when a tightly-related sibling group exists. The `frontend/src/components/` directory no longer exists; recreating it for `ProjectDetail` siblings is regression.
  - `docs/mvp-scope.md` is no longer a checklist or scope authority. It is a redirect document. Future scope-check writers should update the Out-of-scope shortcut list there if a new class of "obviously out of scope" emerges; they MUST NOT re-add a Phase-N implementation checklist.
  - `frontend/src/utils/planningConnectionPresets.ts` is the single source for Connection Template presets across both `ModelSettings.tsx` and `AccountBindings.tsx`. New providers that speak the OpenAI-compatible chat-completions wire should be added there, not to a second preset list.
- **Source**: This PR. Tests at `planningConnectionPresets.test.ts` plus unchanged 88 prior frontend tests (17 files / 93 tests total green); `make lint-governance` passes (the pre-existing "Scope: decision tracking" duplication warning is the accepted one from MIGRATION.md).

## 2026-04-22: Planning Workspace is the per-project primary surface [agent:feature-planner]

- **Context**: Phase 2 shipped across five PRs: S1 `#5` (structural split of `PlanningTab.tsx` into siblings under `pages/ProjectDetail/planning/`), S2 `#6` (AttentionRow at workspace top), S3 `#7` (evidence-linked candidate review), S4 `#8` (applied-task lineage lane with new `GET /api/projects/:id/task-lineage` endpoint), and S5 (this slice: rename + ratify + deep-link polish). The shape is now concrete and the original design doc D2 decision said the ratifying ADR lands at the end of S5.
- **Decision**: The Planning Workspace (formerly the "Planning" tab, renamed to **"Workspace"** in the project rail) is the per-project primary landing surface. `ProjectDetail`'s default `tab` state is now `'planning'` rather than `'overview'`; operators land directly on the surface that answers "what needs my review?" via the AttentionRow. Overview remains at its tab index for read-only status browsing. The tab index and query-param contract are unchanged so deep links like `?tab=overview` continue to resolve. The Workspace composes seven siblings under `frontend/src/pages/ProjectDetail/planning/`: AttentionRow → RequirementIntake → RequirementQueue → PlanningLauncher → PlanningRunList → CandidateReviewPanel → AppliedLineage, all driven by `usePlanningWorkspaceData`. Applied-lineage rows are fully deep-linked across the four-tier chain (`requirement → run → candidate → task`) via a `pendingSelection` state machine that survives the three-step async load chain (select requirement → load runs → auto-select pending run → load candidates → auto-select pending candidate).
- **Alternatives considered**: (1) Keep the "Planning" label and leave Overview as default — rejected; the design doc's §1 problem statement is that operators can't find "what's blocked on my review" and keeping the old defaults preserves the problem. (2) Introduce a new `/projects/:id/workspace` route — rejected in §5 of the design doc for fragmentation reasons; preserving tab-index stability was explicit. (3) Defer the run / candidate deep-links past Phase 2 — rejected because the original design §7-S4 promised "each segment clickable" and the pending-selection state machine is small (≈30 LOC in the hook, consumed by two existing auto-select effects). (4) Relocate grandfathered `components/*Tab.tsx` under `pages/ProjectDetail/` for consistency — rejected as out of scope; the 2026-04-22 Tier-3 sibling rule applies to *new* features, existing components remain where they are.
- **Constraints introduced**:
  - New product features that extend the Planning Workspace MUST land as siblings under `frontend/src/pages/ProjectDetail/planning/` and register through `usePlanningWorkspaceData`. Growing any existing sibling past ~500 LOC (CandidateReviewPanel is the current high-water mark at ~440 LOC post-S3) requires a secondary split in the same PR.
  - `PlanningTab.tsx` is a thin shell. It MUST stay under 300 LOC so the acceptance criterion that protected S1 continues to guard against the return of the 1318-LOC monolith. The post-S5 high-water mark is 242 LOC; the 300 LOC ceiling leaves headroom for routine composition growth (new siblings, new workspace-level props) without enabling a slide back toward monolithic logic. The original S1 target was 200 LOC; S2 (AttentionRow derivations, jump helpers) and S5 (URL-sync, setTab wrapper) legitimately added shell-level glue that warrants the bump. Any PR that pushes the shell over 300 LOC is expected to extract the growth into a sibling or hook in the same PR.
  - The `pendingSelection` state machine in `usePlanningWorkspaceData` is the canonical cross-tier deep-link mechanism. New deep-link callers (Documents / Drift evidence that wants to deep-link into planning, future cross-project workspaces, etc.) MUST reuse `onSelectLineage(requirementId, runId?, candidateId?)` rather than inventing their own pending triples.
  - `GET /api/projects/:id/task-lineage` is the canonical read path for lineage. Future aggregate views (cross-project lineage dashboards, etc.) MUST extend this endpoint rather than add N+1 joins on the frontend.
  - The `"Planning"` tab label is retired in favour of `"Workspace"`. Any doc / screenshot / agent prompt that referenced the old label should update to the new one. Deep-link URL params stay on `tab=planning` for backwards compatibility.
- **Source**: Phase 2 design doc (`docs/phase2-planning-workspace-design.md`, §5–§9), PRs #4 / #5 / #6 / #7 / #8 / this PR.

## 2026-04-22: ProjectDetail.tsx split shipped; UI test coverage outstanding [agent:application-implementer]

- **Context**: Earlier the same day the Tier-3 decision block (2026-04-22 entry T3.C) deferred splitting `ProjectDetail.tsx` out of the v1 PR, citing the risk of landing a 3000+ line restructure without UI tests. The working tree on `main` now contains the split: `ProjectDetail.tsx` went from ~3206 lines to 737 lines and delegates to eight new components (`ProjectOverviewTab`, `TasksTab`, `DocumentsTab`, `DriftTab`, `AgentsTab`, `PlanningTab`, `SettingsTab`, `SyncStatusPanel`) plus `utils/formatters.ts`. The imports are already wired in `ProjectDetail.tsx` lines 28–35. However the original T3.C blocker — zero UI tests — is still unresolved: `find frontend/src -name '*.test.*' -o -name '*.spec.*'` returns nothing.
- **Decision**: Ratify the split as-is. Treat UI smoke coverage as a must-do follow-up rather than a retroactive rollback: bootstrap `vitest` + `@testing-library/react` + `jsdom` in this PR, land at least one smoke test per new tab/panel component within the next merge window, and block any further behavior change to these components until their smoke test exists. `utils/formatters.ts` gets pure unit tests as part of the same bootstrap.
- **Alternatives considered**: (1) Revert the split and redo it in a testable way — rejected; the split as shipped is structurally correct and reverting would destroy working code for process reasons alone. (2) Require full behavioral test parity before merging any further change — rejected as over-reach; one render-smoke test per component is the realistic bar that pays back T3.C's stated concern without stalling product work.
- **Constraints introduced**: New product work on these eight components requires at least one smoke test covering the new code path in the same PR. `frontend/src/**` now needs to type-check with `vitest/globals` available; this is handled via `tsconfig.json` types or explicit `import { describe, it, expect } from 'vitest'` (project chooses explicit imports to keep tsconfig lean).
- **Supersedes**: T3.C clause in 2026-04-22 "Provider.Generate takes context.Context; OpenAI egress consumes wire.PlanningContextV1; SSE deferred; UI split deferred" (the UI-split-deferred half).

## 2026-04-22: Adopt in-process SSE broker for notifications and planning run state [agent:backend-architect]

- **Context**: The same-day Tier-3 decision block (T3.D) explicitly deferred SSE/WebSocket transport, keeping 20s polling plus `visibilitychange` plus the `anpm:refresh-notifications` window event. The working tree already contains a new `backend/internal/events/broker.go` (per-user `sync.RWMutex`-guarded pub/sub with buffered 8-slot channels and silent slow-consumer drop), `handlers/notifications.go` has a `GET /api/notifications/stream` SSE endpoint using `text/event-stream` + `http.Flusher`, and `backend/cmd/server/main.go` wires `notificationBroker := events.NewBroker()` into `notificationStore.SetBroker(...)` and `notificationHandler.WithBroker(notificationBroker, sessionStore)`.
- **Decision**: Adopt SSE as the primary push transport for notifications and planning-run state changes. Keep 20s polling + `visibilitychange` + `anpm:refresh-notifications` as a fallback path (clients without EventSource, proxies that break long-lived connections, or disabled JS). EventSource cannot set custom auth headers, so the stream endpoint accepts a session token via `?token=` query param and must validate it against `SessionStore`.
- **Alternatives considered**: (1) WebSockets — rejected; full-duplex is unnecessary for notifications, and SSE is strictly simpler (plain HTTP, auto-reconnect, text framing). (2) Long polling — rejected; SSE gives better latency for the same operational complexity. (3) External broker (Redis pub/sub, NATS) — rejected for v1 because the deployment target is single-process; switching is a horizontal-scale trigger, not a day-one requirement.
- **Constraints introduced**: The `events.Broker` is per-process and in-memory. Any future horizontal scaling of the server MUST either pin a user's SSE connections to one instance via sticky sessions or replace the broker with a distributed pub/sub. Slow-consumer events are dropped silently — this is acceptable for UI freshness signals but MUST NOT be used for state that is authoritative only via the stream. The `?token=` query param path is strictly limited to the SSE endpoint; other routes continue to require header auth. The `anpm:refresh-notifications` event name remains stable for fallback consumers.
- **Supersedes**: T3.D clause in 2026-04-22 "Provider.Generate takes context.Context; OpenAI egress consumes wire.PlanningContextV1; SSE deferred; UI split deferred" (the SSE-deferred half).

## 2026-04-22: Dual-runtime mode — local (SQLite) and server (PostgreSQL) [agent:backend-architect]

- **Context**: The 2026-04-14 "Move backend runtime to PostgreSQL now" decision superseded the earlier "SQLite as Phase 1" decision, making PostgreSQL the runtime default. The working tree re-introduces SQLite through `backend/internal/config/workspace.go` and `config.go`: when `DATABASE_URL` is unset and a git repo is detected, `config.Load()` sets `LocalMode=true`, points `DatabaseURL` at `sqlite://$REPO_ROOT/.anpm/data.db`, derives a stable port in `[3100, 3999]` from the repo absolute path, and the server auto-provisions the single local project on startup. Server-mode Docker deployments still run PostgreSQL via `DATABASE_URL`.
- **Decision**: Formally recognise two runtime modes that share one schema and one SQL layer:
  - **Local mode** — activated when `DATABASE_URL` is empty and a `.git` parent is found. Uses SQLite at `$REPO_ROOT/.anpm/data.db`, auto-provisions the single project, runs with `InjectLocalAdmin` middleware (no login), picks a deterministic port. Invoked via `anpm serve` / `make serve` / `scripts/serve.sh`.
  - **Server mode** — activated when `DATABASE_URL` is set. Uses PostgreSQL, full auth, multi-user, multi-project. Invoked via Docker Compose or the standalone `server` binary with explicit environment.
- The pre-existing API-004 rule ("use `database/sql` with a driver that supports both SQLite and PostgreSQL") changes from *migration hedge* to *hard requirement*.
- **Alternatives considered**: (1) Keep PostgreSQL as the only runtime and ship Docker-Compose-only local dev — rejected; `anpm serve` in any git repo is a materially better personal-use onboarding than "install Docker, edit compose file, mount repo." (2) Make local mode use an embedded PostgreSQL (e.g. `embedded-postgres`) — rejected; SQLite's zero-dependency footprint is the entire point of local mode. (3) Keep local-mode SQLite but forbid schema-parity with server mode (e.g. two separate DAOs) — rejected; doubles maintenance cost and defeats the migration story.
- **Constraints introduced**: Every new migration MUST apply cleanly under both SQLite (`modernc.org/sqlite`) and PostgreSQL. PostgreSQL-specific syntax (`$N` placeholders, `JSONB`, GIN indexes, `tsvector` full-text search) either needs a SQLite-side equivalent (e.g. text column + LIKE-based search in local mode) or must be feature-gated behind a mode check. Tests must run against both drivers — the existing `scripts/test-with-postgres.sh` remains; a parallel `go test` path without the PostgreSQL dependency is required. Local mode's auth-bypass (`InjectLocalAdmin`) MUST NEVER activate when `LocalMode=false`; the guard in `main.go` is load-bearing. Local mode responses MUST NOT leak the local-mode flag to the public API surface beyond `/api/meta`, which is designed for CLI/status clients.
- **Supersedes**: Partial supersession of "2026-04-14 Move backend runtime to PostgreSQL now" — PostgreSQL remains the *server-mode* default; it is no longer the absolute default across all invocations.

## 2026-04-22: Single-binary distribution with embedded frontend as primary install path [agent:backend-architect]

- **Context**: README and ARCHITECTURE treat `docker compose up --build` as the canonical install path. The working tree adds `.goreleaser.yml` (builds `server` and `anpm` binaries for linux/darwin/windows × amd64/arm64, runs `npm ci && npm run build && cp frontend/dist backend/internal/frontend/dist` in a pre-build hook), `backend/internal/frontend/frontend.go` + `dist/` (Go-embed of the built SPA), `backend/cmd/anpm/main.go` (CLI with `serve` / `status` / `version` subcommands that delegates to `scripts/serve.sh` when present, else execs the sibling `server` binary directly), and `scripts/serve.sh` (auto-rebuilds backend/frontend when sources are newer than artifacts and then starts the server).
- **Decision**: Adopt the single-binary-plus-embedded-frontend path distributed by goreleaser as the primary install path for personal / single-operator use: `go install` or `curl | sh` one binary, run `anpm serve` inside any git repo, get a working UI on a deterministic localhost port. Docker Compose is retained as the deployment path for multi-user, multi-project server-mode installs.
- **Alternatives considered**: (1) Docker-only distribution — rejected; a single static binary removes a full runtime dependency for the majority target (personal PM on a dev laptop). (2) Separate frontend hosting (static-file CDN / separate npm package) — rejected; embedding keeps "one binary, no CORS, no proxy" UX. (3) Ship `anpm` but not `server` and have `anpm` always embed the server — rejected; production server-mode operators prefer running the `server` binary under systemd / a container without the `anpm` CLI wrapper.
- **Constraints introduced**: The goreleaser pre-build hook MUST run to completion before any Go build step; a release without `backend/internal/frontend/dist/` populated produces a binary that serves an empty UI. Version stamping via `-ldflags "-X main.Version=..."` applies to both `anpm` and `server`. `anpm serve`, `anpm status`, and `make serve` MUST share the same `config.LocalMode` detection and port-derivation logic — divergence produces "status says stopped but server is running" bugs. The embedded frontend path in `backend/internal/frontend/frontend.go` is part of the public install contract: do not rename or relocate without a migration note.

## 2026-04-22: Provider.Generate takes context.Context; OpenAI egress consumes wire.PlanningContextV1; SSE deferred; UI split deferred [agent:backend-architect]

- **Context**: Pre-v1 hardening pass identified three Tier 3 follow-ups from the 2026-04-21 decision: (T3.A) `Provider.Generate` had no `context.Context`, so request cancellation, deadlines, and tracing could not propagate from the HTTP handler into the LLM call; the OpenAI provider built its own `context.Background()`, which leaked the request lifetime. (T3.B) Even with the 2026-04-21 sanitizer fixes, the server-side prompt builder still consumed the internal `PlanningContext` directly and reapplied a subset of `wire` helpers ad hoc; the connector path and the server path therefore had two slightly different egress contracts. (T3.C) `frontend/src/pages/ProjectDetail.tsx` is a single 3206-line function with no component or hook extraction and no UI tests, making any "split" risky to land in the same PR as backend correctness fixes. (T3.D) Real-time UI updates use 20s polling + `visibilitychange` + the `anpm:refresh-notifications` window event; SSE/WebSocket support remains unimplemented.
- **Decision**:
  - (T3.A) `Provider.Generate` now takes `(ctx context.Context, requirement, planningContext, selection)`. `DraftPlanner`, `ContextualPlanner`, `SettingsBackedPlanner`, `Orchestrator.Run`, and `candidateGenerator.Generate` all forward the request `context.Context` down to the provider. `OpenAICompatibleProvider.Generate` now passes that ctx into `http.NewRequestWithContext`, so cancellation and deadlines from the handler propagate end-to-end. The deterministic provider accepts ctx as a no-op for interface uniformity.
  - (T3.B) `OpenAICompatibleProvider.Generate` now builds a sanitized `*wire.PlanningContextV1` via a new internal `sanitizeForOpenAIEgress` helper before constructing the prompt: it translates the internal `PlanningContext` to wire types, runs `wire.SanitizePlanningContextV1`, and applies `wire.ReduceSources` against `wire.DefaultLimits().MaxSourcesBytes`. The user prompt and all `compactX*FromWire` helpers consume this sanitized DTO. The 256 KiB request-body guard from 2026-04-21 remains as a defense-in-depth check on the marshalled HTTP body. The server- and connector-egress paths are now governed by the same wire contract end-to-end.
  - (T3.C) Splitting `ProjectDetail.tsx` is deferred out of the v1 PR. The file has no UI tests and the state graph is densely shared; landing a 3000+ line restructure under release pressure carries a high regression risk that outweighs the maintainability gain before v1 ships.
  - (T3.D) SSE/WebSocket transport for notifications and run-state updates remains explicitly deferred. The current 20s polling + `visibilitychange`-triggered fetch + `anpm:refresh-notifications` custom event is sufficient for v1's expected single-operator workflow.
- **Alternatives considered**: (1) Make `Provider.Generate` accept `*wire.PlanningContextV1` directly instead of `PlanningContext` — rejected for v1 because the deterministic provider scores against richer internal data than the wire DTO exposes, and the goal of T3.B (single egress contract) is achieved by sanitizing inside the LLM provider rather than at the interface boundary. (2) Land a partial `ProjectDetail.tsx` split (extract one panel) — rejected; partial splits without a target architecture create churn for marginal benefit and would still leave the file >2500 lines. (3) Adopt SSE for v1 — rejected; adds a new transport, reconnection, and auth surface for a feature single users can replicate with a 20s poll.
- **Constraints introduced**:
  - All `Provider` implementations MUST respect `ctx.Done()` for any I/O operation. Long-running providers MUST use `http.NewRequestWithContext` or equivalent.
  - All future server-side LLM providers MUST sanitize through `wire` (or an explicitly approved equivalent) before egress; ad hoc redaction inside provider code is no longer acceptable.
  - `ProjectDetail.tsx` is on the post-v1 refactor list. New product features added to this page MUST be added as siblings (extracted components or hooks under `frontend/src/pages/ProjectDetail/`) rather than appended to the existing function, to avoid further growth.
  - Polling cadence (20s) and the `anpm:refresh-notifications` event name remain part of the cross-page contract; SSE migration is allowed post-v1 only if it preserves the same event name as a fallback or replaces it via a documented migration step.

## 2026-04-25: Phase 6b — role-dispatch task execution loop closed

- **Context**: Phase 5 established `execution_role` on `backlog_candidates` as a hint for which prompt role a connector should run. Phase 6b closes the loop by wiring up `dispatch_status`/`execution_result` columns on `tasks` and two new connector-authenticated endpoints that let a paired connector atomically claim a queued task, invoke the CLI, and submit the result back.
- **Decision**: Add `dispatch_status TEXT NOT NULL DEFAULT 'none'` and `execution_result JSONB` to `tasks` (migration 029). `dispatch_status = 'queued'` is set at task creation when `source` starts with `role_dispatch:`. Ownership is enforced via the `project_members` table throughout (there is no `projects.user_id` column). The atomic claim uses `BEGIN IMMEDIATE` (SQLite) / `FOR UPDATE SKIP LOCKED` (Postgres) via the existing `database.Dialect` pattern. The connector service `RunOnceTask` method integrates into the existing polling loop alongside `RunOnce` (planning run poll). Frontend `DispatchStatusBadge` renders inline next to the task title; `completed` state is expandable to show file paths. `error_kind` validation reuses the existing `AllowedErrorKinds` allowlist from planning runs.
- **Alternatives considered**: Lease-expiry TTL for tasks (matching planning runs) — deferred; task execution is typically short-lived and the connector loop already handles timeouts via context cancellation. A separate `task_dispatch_log` table for audit history — deferred; `execution_result` JSON on the task row is sufficient for Phase 6b. Role catalog enforcement in the claim handler — deferred; `prompts.Exists` check is in `RunOnceTask` on the connector side, not the server side, because the server does not hold the prompts directory.
- **Constraints introduced**: Only tasks with `dispatch_status = 'running'` may have results submitted; any other state returns 400. Ownership check uses `project_members` JOIN (not `projects.user_id`). The `NewTaskStoreWithDialect` constructor must be used when dispatch methods are needed; `NewTaskStore` (no dialect) continues to work for code that does not use dispatch. `main.go` uses `NewTaskStoreWithDialect` to ensure the dispatch path is always available in production.
- **Source**: [agent:backend-architect]

## 2026-04-27: Phase 6c PR-3 — LLM role router (suggest-only, advisory) [agent:backend-architect]

- **Context**: Operators manually selecting a role for each backlog candidate is friction-heavy and error-prone. Phase 6c PR-3 adds an advisory LLM routing layer that proposes a role without removing the operator's explicit confirmation step.
- **Decision**: `POST /api/backlog-candidates/:id/suggest-role` runs the dispatcher prompt (in `prompts/meta/dispatcher.md`, category=meta) through the existing `invokeBuiltinCLI` safety boundary and returns `{role_id, confidence, reasoning, alternatives[]}` without persisting the result (API-008: advisory-only). The dispatcher meta-role is added to the catalog (`Category: "meta"`) but filtered out from `GET /api/roles` so it never appears in the role selection dropdown. `SuggestRole` is exported from the `connector` package (wraps `invokeBuiltinCLI`) rather than the `planning` package to avoid leaking subprocess invocation outside the connector boundary. A `roleSuggesterFn` function type is injected into `PlanningRunHandler.WithRoleSuggester(...)` for testability. Frontend: "💡 Suggest role" button appears inside the role_dispatch radio branch; suggestion pre-fills the dropdown + tooltip shows `reasoning` + `alternatives`; operator must still click Save (UI-008). `ErrorKindRouterNoMatch = "router_no_match"` added to `AllowedErrorKinds` so frontend can render advisory UI on no-match. `Auto-apply (mode=role_dispatch_auto)` deferred to Phase 6d per user decision B2.
- **Constraints introduced**: Dispatcher result is never persisted in 6c; audit row (if any) is written as `actor_kind="user"` after operator confirmation, not `actor_kind="router"`. Prompt embed directive updated to `//go:embed *.md roles/*.md meta/*.md`. `TestMetaRolesHavePromptFiles` added alongside `TestCatalogMatchesPromptDir` to verify the meta/ directory.
- **Source**: phase6c-plan.md §3.3; PR-3 implementation 2026-04-27.

## 2026-04-27: Phase 6c PR-4 — connector activity tracking

- **Context**: Connector execution is opaque to the browser: the user can see that a connector is online (heartbeat), but not what phase it is currently executing (idle, planning a run, dispatching a task, etc.). The UI needs a lightweight way to show real-time progress without adding another polling loop or requiring the connector to push into the DB synchronously on every phase change.
- **Decision**: Add a `POST /api/connector/activity` endpoint (connector-token auth) where the connector reports its current `ConnectorActivity` struct. The server maintains an in-memory `activity.Hub` (fan-out pub/sub, same non-blocking drop pattern as `events.Broker`) that holds the latest snapshot per connector and pushes SSE to the browser via `GET /api/me/local-connectors/:id/activity-stream`. A polling fallback `GET /api/me/local-connectors/:id/activity` is also provided. The hub persists snapshots to two new columns on `local_connectors` (`current_activity_json`, `current_activity_at`, migration 031) asynchronously (fire-and-forget goroutine). At startup, persisted snapshots are restored into the hub so state survives a server restart. The connector side introduces `ActivityReporter` with a 500ms coalesce window (same-phase step changes are merged; phase changes flush immediately). `Service.ActivityReporter` is an optional field — nil means no reporting (backwards-compatible). `"online"` definition is `last_seen_at` within 90 seconds (3× the 30s heartbeat interval), consistent with `LocalConnectorLivenessWindow`.
- **Alternatives considered**: Store activity in a dedicated table per update — rejected because activity is ephemeral and high-frequency; column-on-connector is sufficient for restart recovery. Rate-limit SSE connections — deferred to Phase 6d. Write to `actor_audit` on each activity change — explicitly rejected per the Phase 6c plan (activity is operational telemetry, not an authoring lifecycle event). Synchronous DB persist on POST — rejected because it would block the connector's hot polling loop.
- **Constraints introduced**: `activity.Hub` is a new top-level package under `internal/activity`; no circular imports. `Persister` is an interface so the hub is testable without a DB. Down migration 031 is a no-op (SQLite `DROP COLUMN` not supported pre-3.35). The `active-connectors` aggregate endpoint is project-scope best-effort (connectors are not yet project-scoped; it returns all user connectors with activity).
- **Source**: [agent:backend-architect]

## 2026-04-27: Phase 6c PR-4 — frontend activity hook + badge [agent:application-implementer]

- **Context**: The backend activity hub and SSE endpoint (PR-4 backend) emit `activity` events per connector. The frontend needs a hook that follows the SSE-primary / polling-fallback pattern and a reusable badge component with three density variants.
- **Decision**: `useConnectorActivity(connectorId)` opens an `EventSource` to `/api/me/local-connectors/:id/activity-stream` and polls `/api/me/local-connectors/:id/activity` every 15 seconds as fallback; stale state (>90s since last update) is surfaced as `source='stale'` for dimmed rendering. Component-level `EventSource` (not App-level) is acceptable here because each badge tracks a distinct connectorId and there is at most one active run per planning session in the local-mode dogfood scope. `ConnectorActivityBadge` renders compact (inline dot + phase name), standard (dot + phase + subject title), or full (expanded with step + stale chip). `PlanningRunList` shows a standard badge on active runs (`status=queued|running`) that have a `connector_id`. Tasks do not carry `connector_id` so task-level integration is deferred until the schema adds it.
- **Constraints introduced**: `ConnectorPhase` union type and `ConnectorActivityResponse` / `ActiveConnectorEntry` interfaces added to `src/types/index.ts`. API functions `getConnectorActivity`, `listActiveConnectors`, and `connectorActivityStreamURL` added to `src/api/client.ts`. The hook cleans up `EventSource` on unmount; the stale timer fires every 10s to detect dropped connections.
- **Source**: phase6c-plan.md §3.4.5; frontend PR-4 implementation 2026-04-27.

## 2026-04-27: Phase 6c PR-5 — dogfood notes + operating-rules + DECISIONS archival [agent:application-implementer]

- **Context**: Phase 6c closes 5 PRs (PR-1 catalog+safety, PR-2 authoring, PR-3 router, PR-4 activity, PR-5 docs+archival). PR-5 is the validation + documentation consolidation slice.
- **Decision**: `docs/phase6c-dogfood-notes.md` documents 7 dogfood steps covering the full role-dispatch path (happy-path, error induction, router suggest UX, activity badge observation). `docs/operating-rules.md` gains a "Role-dispatch safety + visibility model" section covering L0/L1/L2 trigger conditions and activity SSE constraints. `DECISIONS.md` entries before 2026-04-22 are moved to `DECISIONS_ARCHIVE.md` (file was 135KB, well above the 30KB threshold). The affected-tests-only script (`scripts/test-affected.sh` + CI path-filter) ships alongside PR-5 because test infrastructure is a companion deliverable for the phase.
- **Constraints introduced**: DECISIONS.md archival boundary is 2026-04-22 00:00 UTC; entries on or after that date stay in the active file. `DECISIONS_ARCHIVE.md` receives a new `## Archived on 2026-04-27` section header. Phase 6d trigger conditions are documented in `docs/phase6c-plan.md §9`; no `phase6d-plan.md` is created yet per user decision.
- **Source**: phase6c-plan.md §3.5; PR-5 implementation 2026-04-27.

## 2026-04-27: Phase 3B PR-2 — context-snapshot endpoint + snapshot save on ClaimNextRun [agent:backend-architect]

- **Context**: PR-1 added the `planning_context_snapshots` table and `PlanningContextV2` wire contract. PR-2 wires snapshot saving and exposes the snapshot as a first-class API resource.
- **Decision 1 — snapshot save location**: The spec placed snapshot saving in the orchestrator, but the orchestrator does not call `BuildContextV1` — only `LocalConnectorHandler.ClaimNextRun` does (for `local_connector` execution mode runs). Rather than introduce a context builder dependency into the orchestrator (which would complicate its dependency graph and add orchestrator responsibility it doesn't own), snapshot saving is wired in `ClaimNextRun` immediately after `BuildContextV1` succeeds. The `SnapshotSaver` interface + `WithSnapshotSaver` fluent setter ARE added to `Orchestrator` as specified (for future use / non-local paths), but the actual save goes through a `contextSnapshotSaver` interface on `LocalConnectorHandler`. Fire-and-forget semantics preserved: save failure is logged, never propagates to the caller.
- **Decision 2 — V2 envelope defaults**: `UpgradeV1ToV2` is called with `role=""` (Phase 6c PR-3 will fill this), `IntentModeImplement` (default for local_connector runs), and `scale.EstimateTaskScale(title, description)` from the requirement.
- **Decision 3 — endpoint**: `GET /api/planning-runs/:id/context-snapshot` is added to `PlanningRunHandler` (not a new handler struct) via `WithContextSnapshotStore(ContextSnapshotGetter)`. Runs with no snapshot return HTTP 200 `{available: false}` (older runs, non-local-connector runs). `?raw=1` returns the raw `PlanningContextV2` JSON blob. Auth follows the same project-member pattern as `PlanningRunHandler.Get`.
- **Constraints introduced**: `wire` package remains a leaf. `ContextSnapshotGetter` interface lives in `handlers` (not `store`) to avoid store→handlers import. `ContextSnapshotResponse` is exported for test assertions. No schema or migration changes (032 and 033 already existed from PR-1).
- **Source**: [agent:backend-architect] Phase 3B PR-2 implementation 2026-04-27.



## 2026-04-27: Phase 7 — connector dispatch pipeline + file writing + requeue

- **Context**: Phase 6c wired role-dispatch task creation and the activity badge, but the connector had no end-to-end task execution loop. Phase 7 adds the full claim → execute → submit cycle for `role_dispatch` tasks, including file writing from execution results, a requeue endpoint for failed tasks, and SSE cap enforcement.
- **Decision 1 — progress ticker `defer close(done)`**: Each execution function (`RunOnceTask`, `RunOnce`) creates a ticker goroutine signalled by a `done` channel. The `close(done)` call is placed as `defer close(done)` immediately after channel creation so the goroutine exits on all paths including panic. Without `defer`, a panic in `invokeBuiltinCLI` or `ExecuteBuiltin` would leak the goroutine indefinitely.
- **Decision 2 — `applyExecutionResultFiles` best-effort contract**: File writes from the `files[]` array in a task result are best-effort: individual file errors are logged to stderr but never fail the task submission. The server receives the full JSON result regardless. `files_applied` count is included in `ExecutionResult` so the UI can show how many files were written. `repoPath` must be an absolute path; non-absolute or empty values are rejected before any write attempt.
- **Decision 3 — task stuck in `running` on `SubmitTaskResult` failure**: If a connector submits a task result but the HTTP call fails (network error, 5xx), the task stays in `dispatch_status=running` on the server because `RequeueDispatch` only accepts `dispatch_status=failed` tasks. There is currently no server-side lease expiry for `running` tasks. **Mitigation (Phase 8)**: add a background sweep that ages `running` tasks older than `N × maxRoleTimeout` to `failed`. Until then, stuck tasks require a direct DB update by the operator.
- **Decision 4 — `PurgeIdle` exported for testing**: The idle-entry purge method is exported as `PurgeIdle()` (capital P) so tests can invoke it directly without waiting for the background ticker. The `StartPurge` background goroutine calls `PurgeIdle` on each tick.
- **Decision 5 — migration 035 backfill strategy**: Migration 035 inserts `project_members` rows for all existing projects and the `local-admin` user so `ClaimNextDispatchTask`'s project-membership JOIN finds the correct tasks. The down migration is a no-op (`SELECT 1`) because inserted rows cannot be distinguished from rows added post-migration; this is documented inline and is acceptable because `project_members` rows are non-destructive to add.
- **Constraints introduced**: `RequeueDispatch` requires authenticated session (explicit nil-user guard in handler, returns 401). `applyExecutionResultFiles` rejects non-absolute `repoPath`. `PurgeIdle` is exported. Phase 8 must add a `running`-task lease expiry to close the gap from Decision 3.
- **Source**: Phase 7 implementation 2026-04-27.
